---
phase: 06-redemption-catalystmod-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Libs/Redemption.dll
  - Libs/CatalystMod.dll
  - build.txt
  - BossArenaSubWorld.csproj
autonomous: true
requirements: [MOD-03, MOD-04]

must_haves:
  truths:
    - "Libs/Redemption.dll and Libs/CatalystMod.dll exist locally (gitignored via the existing Libs/ rule) and the project builds successfully (dotnet build exit 0) with both wired in as compile-time-only weak references, mirroring the existing SpiritMod/CalamityMod Reference block pattern"
    - "No Redemption or CatalystMod type is referenced anywhere in code yet -- this plan only proves both references resolve at compile time, exactly like 04-01/05-01's Task 1 precedent"
  artifacts:
    - path: "Libs/Redemption.dll"
      provides: "Local compile-time assembly for Redemption weak reference (copied from the already-extracted ModReader/Redemption/Redemption.dll, v0.8.0.4501)"
    - path: "Libs/CatalystMod.dll"
      provides: "Local compile-time assembly for CatalystMod weak reference (extracted via scripts/extract_tmod.py from the Steam Workshop-cached .tmod, since CatalystMod is not present in the local Mods/ folder -- v1.1.8)"
    - path: "build.txt"
      provides: "Declares weakReferences includes Redemption@0.8.0.4501 and CatalystMod@1.1.8"
      contains: "Redemption@0.8.0.4501, CatalystMod@1.1.8"
    - path: "BossArenaSubWorld.csproj"
      provides: "Compile-time-only Reference blocks to Libs/Redemption.dll and Libs/CatalystMod.dll, mirroring the existing SpiritMod/CalamityMod blocks"
      contains: "Reference Include=\"Redemption\""
  key_links:
    - from: "build.txt weakReferences entry (Redemption@0.8.0.4501, CatalystMod@1.1.8)"
      to: "BossArenaSubWorld.csproj Reference HintPath (Libs\\Redemption.dll, Libs\\CatalystMod.dll)"
      via: "Both declare the same two mods -- build.txt for tModLoader's runtime weak-dependency resolution, .csproj for dotnet build's compile-time resolution (plain dotnet build cannot resolve build.txt's modReferences/weakReferences mechanism, per Phase 1 D-02 precedent)"
      pattern: "Reference Include=\"(Redemption|CatalystMod)\""
---

<objective>
Extract/copy `Redemption.dll` and `CatalystMod.dll` into `Libs/`, then wire both as compile-time-only weak references in `build.txt` and `BossArenaSubWorld.csproj`, exactly mirroring the existing `SpiritMod`/`CalamityMod` Reference blocks (Phase 4/5 precedent). This is Phase 6's Wave 0 setup step, unblocking the two integration files built in Plan 02.

Purpose: Both mods' `.dll` files must be locally available for `dotnet build` to resolve compile-time types, since tModLoader's own `weakReferences` runtime-resolution mechanism (declared in `build.txt`) is not understood by the plain `dotnet build`/`dotnet msbuild` command line (confirmed precedent: Phase 1 D-02, Phase 4/5's identical Task 1 pattern).
Output: `Libs/Redemption.dll`, `Libs/CatalystMod.dll` (both gitignored, local-only), updated `build.txt` and `BossArenaSubWorld.csproj`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-redemption-catalystmod-integration/06-CONTEXT.md
@.planning/phases/06-redemption-catalystmod-integration/06-RESEARCH.md
@.planning/phases/06-redemption-catalystmod-integration/06-VALIDATION.md
@CLAUDE.md
@build.txt
@BossArenaSubWorld.csproj
@scripts/extract_tmod.py

<interfaces>
<!-- Existing build.txt / .csproj shape this plan extends. No changes needed to the shape
itself -- only two new entries/blocks mirroring the existing SpiritMod one. -->

Current build.txt:
```
displayName = 보스 진행???�기?�되???�브?�드
author = HiPoDEV
version = 0.1
modReferences = SubworldLibrary
weakReferences = CalamityMod@2.2.4, SpiritMod@1.5.0.44
```
(Comma-separated syntax confirmed working -- Phase 5 tooling note: space-separated fails to parse
with "Invalid mod reference".)

Current BossArenaSubWorld.csproj `<ItemGroup>` (three existing `<Reference>` blocks: SubworldLibrary,
CalamityMod, SpiritMod) -- this plan adds a fourth and fifth block with the identical shape:
```xml
<Reference Include="SpiritMod" Condition="Exists('Libs\SpiritMod.dll')">
    <HintPath>Libs\SpiritMod.dll</HintPath>
    <Private>false</Private>
</Reference>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract/copy Redemption.dll and CatalystMod.dll into Libs/</name>
  <files>Libs/Redemption.dll, Libs/CatalystMod.dll</files>
  <read_first>
    - scripts/extract_tmod.py (the existing, already-cross-validated .tmod extraction tool -- takes tmod_path and out_dir positional args, writes every embedded file including .dll/.pdb into out_dir)
    - .planning/phases/06-redemption-catalystmod-integration/06-RESEARCH.md section "Environment Availability" and "Code Examples" (exact confirmed file paths and the extraction command, including the Python full-path pitfall)
  </read_first>
  <action>
    Redemption's `.dll` is already extracted locally -- copy it directly:
    ```
    mkdir -p Libs
    cp "C:\Users\chang\Documents\My Games\Terraria\tModLoader\ModReader\Redemption\Redemption.dll" "Libs\Redemption.dll"
    ```

    CatalystMod's `.dll` is NOT present in the local `Mods/` folder or `ModReader/`. It must be
    extracted from the Steam Workshop content cache using this project's existing
    `scripts/extract_tmod.py`. **Do not use the bare `python`/`python3` command** -- it resolves
    to a non-functional Windows Store alias stub on this machine. Use the full path:
    ```
    "C:/Users/chang/AppData/Local/Programs/Python/Python313/python.exe" scripts/extract_tmod.py \
      "D:/SteamLibrary/steamapps/workshop/content/1281930/2838015851/2026.6/CatalystMod.tmod" \
      Libs/_catalyst_extract_tmp
    cp "Libs/_catalyst_extract_tmp/CatalystMod.dll" "Libs/CatalystMod.dll"
    rm -rf "Libs/_catalyst_extract_tmp"
    ```

    If the Workshop cache path above no longer exists (e.g. Steam moved the cached depot), locate
    it fresh via the same `libraryfolders.vdf` approach documented in 06-RESEARCH.md's Pitfall 1
    before falling back to any other source.
  </action>
  <verify>
    <automated>test -f "Libs/Redemption.dll" && test -f "Libs/CatalystMod.dll" && echo BOTH_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `Libs/Redemption.dll` exists locally and has non-zero size
    - `Libs/CatalystMod.dll` exists locally and has non-zero size
    - `Libs/_catalyst_extract_tmp/` temp directory no longer exists (cleaned up)
    - Neither new `.dll` is staged/tracked by git (already covered by the existing `Libs/` gitignore rule -- confirm via `git status` showing no `Libs/*.dll` entries)
  </acceptance_criteria>
  <done>Both Libs/Redemption.dll and Libs/CatalystMod.dll exist locally, ready for the .csproj Reference blocks added in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Redemption and CatalystMod weak references in build.txt and BossArenaSubWorld.csproj</name>
  <files>build.txt, BossArenaSubWorld.csproj</files>
  <read_first>
    - build.txt (current state -- the exact weakReferences line to extend)
    - BossArenaSubWorld.csproj (current state -- three existing Reference blocks, the exact shape to mirror twice more)
    - .planning/phases/06-redemption-catalystmod-integration/06-RESEARCH.md section "Code Examples" (exact confirmed weakReferences line and .csproj Reference block text)
  </read_first>
  <action>
    Edit `build.txt`'s existing `weakReferences` line to add both new mods as comma-separated
    entries (comma-separated syntax confirmed working per Phase 5's tooling note -- space-separated
    fails with "Invalid mod reference"):
    ```
    weakReferences = CalamityMod@2.2.4, SpiritMod@1.5.0.44, Redemption@0.8.0.4501, CatalystMod@1.1.8
    ```

    Edit `BossArenaSubWorld.csproj`'s existing `<ItemGroup>` (the one already containing the
    SubworldLibrary/CalamityMod/SpiritMod `<Reference>` blocks) to add two more blocks, mirroring
    the existing three exactly:
    ```xml
    <!--
        Compile-time-only weak reference to Redemption, declared as a weak runtime
        dependency via `weakReferences = ... Redemption@0.8.0.4501` in build.txt (per D-01: the
        mod must still load with Redemption absent). Libs/Redemption.dll is copied directly from
        the already-extracted ModReader/Redemption/Redemption.dll (not committed, see .gitignore)
        and never copied into this mod's own output (Private=false).
    -->
    <Reference Include="Redemption" Condition="Exists('Libs\Redemption.dll')">
        <HintPath>Libs\Redemption.dll</HintPath>
        <Private>false</Private>
    </Reference>

    <!--
        Compile-time-only weak reference to CatalystMod, declared as a weak runtime
        dependency via `weakReferences = ... CatalystMod@1.1.8` in build.txt (per D-01: the
        mod must still load with CatalystMod absent). Libs/CatalystMod.dll is extracted from the
        Steam Workshop-cached .tmod via scripts/extract_tmod.py (not committed, see .gitignore)
        and never copied into this mod's own output (Private=false).
    -->
    <Reference Include="CatalystMod" Condition="Exists('Libs\CatalystMod.dll')">
        <HintPath>Libs\CatalystMod.dll</HintPath>
        <Private>false</Private>
    </Reference>
    ```

    After both edits, run (in order, from the project root):
    ```
    dotnet restore
    dotnet build BossArenaSubWorld.csproj
    ```
    The build must succeed (exit code 0) even though no code yet references any Redemption or
    CatalystMod type -- this task only proves both references resolve.
  </action>
  <verify>
    <automated>cd "C:\Users\chang\Documents\My Games\Terraria\tModLoader\ModSources\BossArenaSubWorld" && dotnet build BossArenaSubWorld.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `build.txt` contains the exact string: Redemption@0.8.0.4501
    - `build.txt` contains the exact string: CatalystMod@1.1.8
    - `BossArenaSubWorld.csproj` contains the string: Reference Include="Redemption"
    - `BossArenaSubWorld.csproj` contains the string: Reference Include="CatalystMod"
    - `dotnet build BossArenaSubWorld.csproj` exits with code 0
  </acceptance_criteria>
  <done>build.txt declares both new weak references, BossArenaSubWorld.csproj resolves compile-time-only References to both new Libs/*.dll files, and the project still builds successfully with no Redemption/CatalystMod-touching code yet.</done>
</task>

</tasks>

<verification>
`dotnet build BossArenaSubWorld.csproj` exits 0 after both tasks, with `Libs/Redemption.dll` and `Libs/CatalystMod.dll` present locally and both referenced (but unused) in the compiled output.
</verification>

<success_criteria>
- Libs/Redemption.dll and Libs/CatalystMod.dll exist locally
- build.txt and BossArenaSubWorld.csproj both declare Redemption and CatalystMod as weak/compile-time-only references
- Project builds successfully with zero errors
- Plan 02's integration files can now compile against both assemblies
</success_criteria>

<output>
After completion, create `.planning/phases/06-redemption-catalystmod-integration/06-01-SUMMARY.md`.
</output>
