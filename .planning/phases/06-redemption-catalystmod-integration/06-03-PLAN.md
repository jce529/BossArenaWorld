---
phase: 06-redemption-catalystmod-integration
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: []
autonomous: false
requirements: [MOD-03, MOD-04]

must_haves:
  truths:
    - "Live in-game test confirms Redemption.Globals.RedeBossDowned.downedThorn == true after using the carrier item in the main world following a real Thorn kill in the subworld, and Thorn's chat message + Alignment change replay correctly"
    - "Live in-game test confirms CatalystMod.WorldDefeats.downedAstrageldon == true after using the carrier item in the main world following a real Astrageldon kill in the subworld, and MetanovaGenerator.Generate()'s ore-vein tiles are visibly generated"
    - "The mod continues to load and run safely with Redemption disabled -- no JITException naming Integrations/RedemptionIntegration.cs in Logs/client.log"
    - "The mod continues to load and run safely with CatalystMod disabled -- no JITException naming Integrations/CatalystIntegration.cs in Logs/client.log"
  artifacts: []
  key_links: []
---

<objective>
Empirically verify, via live in-game checkpoints on a backed-up throwaway world, that Plan 02's two integrations actually work end-to-end at runtime -- not just that they compile. Covers Redemption/CatalystMod's Success Criteria 1-3: Thorn's downed-flag+side-effect application, Astrageldon's downed-flag+WorldGen application, and both mods' disabled-load safety.

Purpose: Closes MOD-03/MOD-04's live-verification gap (06-VALIDATION.md: "manual-only, dedicated throwaway world" -- no automated tModLoader in-game test harness exists in this project, per Phase 1-5/9 precedent) and Phase 6's Success Criteria 1-3. Neither Redemption nor CatalystMod is currently installed/enabled in the local Mods folder (06-RESEARCH.md Environment Availability) -- Task 1 covers getting both mods enabled as its first step.
Output: Two completed manual checkpoint results (Thorn/Astrageldon downed-flag-applies confirmed; Redemption-disabled/CatalystMod-disabled safety confirmed), recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-redemption-catalystmod-integration/06-CONTEXT.md
@.planning/phases/06-redemption-catalystmod-integration/06-RESEARCH.md
@.planning/phases/06-redemption-catalystmod-integration/06-VALIDATION.md
@docs/WORLD_BACKUP_GUIDANCE.md
@Integrations/RedemptionIntegration.cs
@Integrations/CatalystIntegration.cs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live Thorn and Astrageldon downed-flag application checkpoint</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/RedemptionIntegration.cs (RegisterThorn/ApplyThornDowned/IsThornDowned -- the exact side effects this checkpoint confirms replay correctly: chat message, RedeWorld.Alignment += 2, ChaliceAlignmentUI dialogue)
    - Integrations/CatalystIntegration.cs (RegisterAstrageldon/ApplyAstrageldonDowned/IsAstrageldonDowned -- the exact WorldGen side effect this checkpoint confirms: MetanovaGenerator.Generate() ore-vein generation, and the non-`-1` gameEventId)
    - docs/WORLD_BACKUP_GUIDANCE.md (backup procedure -- follow before testing on a real/dedicated throwaway world, per VERIFY-02)
    - .planning/phases/06-redemption-catalystmod-integration/06-RESEARCH.md "Environment Availability" (neither mod is currently enabled in the local Mods folder -- confirmed missing dependencies with a low-effort fallback)
  </read_first>
  <action>
    Part A -- Enable both mods:
    1. If not already subscribed, subscribe to Redemption (Steam Workshop id 2893332653) and
       CatalystMod (Steam Workshop id 2838015851) via the in-game Mod Browser or Steam Workshop
       page, then launch tModLoader once so it syncs the Workshop cache into the local `Mods\`
       folder.
    2. In Mod Configuration, enable both Redemption and CatalystMod alongside the existing
       CalamityMod, SpiritMod, SubworldLibrary, CheatSheet, BossChecklist, and
       BossArenaSubWorld, then reload mods.
    3. Confirm mods load with no error (this alone is not the disabled-safety checkpoint --
       that is Task 2 below).

    Part B -- World backup:
    4. Follow docs/WORLD_BACKUP_GUIDANCE.md and back up (or create a fresh dedicated throwaway)
       world before testing.

    Part C -- Thorn:
    5. Acquire a `HeartOfThorns` item (e.g. via CheatSheet's item-spawn menu, already installed).
    6. Right-click the Test1 portal tile while holding `HeartOfThorns` to redirect into the
       boss-arena subworld (per SUBW-02/SUBW-03); confirm Thorn auto-summons on arrival (SUBW-04).
    7. Kill Thorn; confirm a `BossCoreItem` tagged `redemption:thorn` drops (only inside the
       subworld, per DROP-02).
    8. Return to the main world using SubworldLibrary's Return mechanism.
    9. Use the dropped `BossCoreItem`; confirm:
       - `Redemption.Globals.RedeBossDowned.downedThorn` becomes `true` (check via Boss
         Checklist's tracker UI, or CheatSheet's variable inspector if available)
       - The chat message from `Mods.Redemption.StatusMessage.Progression.ThornDowned` appears
       - `RedeWorld.Alignment` increased by 2 (observable via Redemption's own Chalice UI if
         exposed, or accept the code-level guarantee if not directly visible)
    10. Re-use the same (now-consumed or retained, per BossCoreItem's D-02 policy) item path and
        confirm no duplicate chat message / no double-apply (APPLY-04 idempotency).

    Part D -- Astrageldon:
    11. Acquire an `AstralCommunicator` item (e.g. via CheatSheet).
    12. Right-click the Test1 portal tile while holding `AstralCommunicator` to redirect into the
        boss-arena subworld; confirm Astrageldon auto-summons on arrival.
    13. Kill Astrageldon; confirm a `BossCoreItem` tagged `catalyst:astrageldon` drops.
    14. Return to the main world.
    15. Use the dropped `BossCoreItem`; confirm:
        - `CatalystMod.WorldDefeats.downedAstrageldon` becomes `true`
        - Ore-vein tiles are visibly generated near the player by `MetanovaGenerator.Generate()`
        - No JITException or runtime exception occurs during `NPC.SetEventFlagCleared`
    16. Re-use the item path again and confirm no double-apply (APPLY-04 idempotency, no
        duplicate ore-vein generation).
  </action>
  <what-built>
    Plan 02 registered Thorn (`redemption:thorn`) and Astrageldon (`catalyst:astrageldon`) into
    the existing BossRegistry/SummonItemRegistry pipeline, using each mod's actual public-static-
    field downed-tracking API confirmed via decompiled source. This task verifies the full
    subworld-kill -> carrier-item-drop -> main-world-apply pipeline actually works at runtime for
    both bosses, including their real side effects (Thorn's chat/Alignment/dialogue broadcast,
    Astrageldon's WorldGen ore-vein generation), not just that the code compiles.
  </what-built>
  <how-to-verify>
    Follow the 16 numbered steps in the action above (Parts A-D in order).
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader boss-kill/carrier-item behavior (per 06-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - Redemption and CatalystMod both subscribed, enabled, and loaded without error
    - World backed up per docs/WORLD_BACKUP_GUIDANCE.md before any live test
    - Thorn killed in the subworld, BossCoreItem tagged redemption:thorn dropped and used in the main world, RedeBossDowned.downedThorn confirmed true, chat message observed
    - Astrageldon killed in the subworld, BossCoreItem tagged catalyst:astrageldon dropped and used in the main world, WorldDefeats.downedAstrageldon confirmed true, ore-vein tiles observed generated
    - Re-using either carrier item does not duplicate any side effect (APPLY-04)
  </acceptance_criteria>
  <done>Thorn's and Astrageldon's full subworld-kill-to-main-world-apply pipelines are empirically confirmed working, including their real side effects, satisfying Phase 6 Success Criteria 1 and 2.</done>
  <resume-signal>Type "both bosses verified" if all 16 steps passed, or describe exactly which step failed and what was observed instead.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Redemption-disabled and CatalystMod-disabled load-safety checkpoint</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/RedemptionIntegration.cs, Integrations/CatalystIntegration.cs (confirm every method touching a Redemption/CatalystMod type carries its own [JITWhenModsEnabled] tag -- the property this checkpoint is actually testing at runtime)
    - .planning/phases/09-biome-dependent-subworld-coverage/09-07-PLAN.md Task 1 (the established precedent for this exact style of checkpoint -- same failure mode, different integration files; that checkpoint caught a real JITException live)
    - .planning/PROJECT.md Key Decisions (the Phase 4 lambda/[JITWhenModsEnabled] JIT-crash lesson, commit 0e19600, and the Phase 9 lesson that lazy construction alone is not sufficient JIT protection)
  </read_first>
  <action>
    Part A -- Redemption disabled:
    1. In tModLoader's Mod Configuration menu, disable Redemption only (leave CatalystMod,
       CalamityMod, SpiritMod, SubworldLibrary, and BossArenaSubWorld enabled), then relaunch
       tModLoader and load into any world.
    2. Confirm tModLoader loads to the main menu and into the world without a JIT crash,
       exception dialog, or mod-load error mentioning "Redemption".
    3. Check `Logs/client.log` for any exception naming `RedemptionIntegration` -- confirm none
       are present.
    4. Confirm BossArenaSubWorld's other, Redemption-independent features (King Slime, Hive
       Mind, Infernon, Astrageldon if CatalystMod is still enabled) still function normally.
    5. Re-enable Redemption.

    Part B -- CatalystMod disabled:
    6. In tModLoader's Mod Configuration menu, disable CatalystMod only (leave Redemption,
       CalamityMod, SpiritMod, SubworldLibrary, and BossArenaSubWorld enabled), then relaunch
       tModLoader and load into any world.
    7. Confirm tModLoader loads to the main menu and into the world without a JIT crash,
       exception dialog, or mod-load error mentioning "CatalystMod".
    8. Check `Logs/client.log` for any exception naming `CatalystIntegration` -- confirm none
       are present.
    9. Confirm BossArenaSubWorld's other, CatalystMod-independent features still function
       normally.
    10. Re-enable CatalystMod, restoring the normal mod list for future phases.
  </action>
  <what-built>
    Plan 02's two integration files isolate every Redemption/CatalystMod type reference behind a
    named, separately-`[JITWhenModsEnabled]`-tagged method, following the locked project-wide
    discipline established in Phase 4 (a real live JITException from an inline lambda) and
    reinforced in Phase 9 (lazy construction alone is not sufficient). This task verifies that
    isolation boundary actually prevents a JIT crash at runtime when each mod is genuinely absent,
    not just that the code looks correctly isolated on review.
  </what-built>
  <how-to-verify>
    Follow the 10 numbered steps in the action above (Part A, then Part B).
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader mod-load JIT behavior (per 06-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - tModLoader launched and loaded into a world successfully with Redemption disabled, no JIT crash or exception dialog observed
    - Logs/client.log contains no Redemption-related exception naming RedemptionIntegration
    - tModLoader launched and loaded into a world successfully with CatalystMod disabled, no JIT crash or exception dialog observed
    - Logs/client.log contains no CatalystMod-related exception naming CatalystIntegration
    - Both mods re-enabled afterward, mod list confirmed restored to normal
  </acceptance_criteria>
  <done>Both integrations are empirically confirmed to load and run safely at runtime with their respective source mod disabled, satisfying Phase 6 Success Criterion 3.</done>
  <resume-signal>Type "mod-disabled safety verified" if both parts loaded cleanly with no relevant exceptions in the log, or paste the exact exception/error otherwise.</resume-signal>
</task>

</tasks>

<verification>
Both checkpoint tasks in this plan have no automated equivalent (per 06-VALIDATION.md -- no in-game test harness exists in this project). Verification is entirely the human-confirmed resume-signals: "both bosses verified" (Task 1) and "mod-disabled safety verified" (Task 2).
</verification>

<success_criteria>
- Live test confirms Thorn's downed flag + side effects apply correctly in the main world (Phase 6 SC1)
- Live test confirms Astrageldon's downed flag + WorldGen side effect apply correctly in the main world (Phase 6 SC2)
- Live test confirms the mod loads and runs safely with Redemption disabled, no JIT crash (Phase 6 SC3)
- Live test confirms the mod loads and runs safely with CatalystMod disabled, no JIT crash (Phase 6 SC3)
- MOD-03 and MOD-04 fully satisfied end-to-end (research + registration + live verification)
</success_criteria>

<output>
After completion, create `.planning/phases/06-redemption-catalystmod-integration/06-03-SUMMARY.md`, recording the actual live test results for both checkpoints.
</output>
