---
phase: 03-bossregistry-bosscoreitem-globalnpc-pipeline-proof-of-concept
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified: []
autonomous: false
requirements: [DROP-02, DROP-03, APPLY-01, APPLY-04]

must_haves:
  truths:
    - "Killing King Slime inside the boss-arena subworld drops a BossCoreItem; killing King Slime outside the subworld does not"
    - "The dropped BossCoreItem survives inventory pickup and the subworld exit trip, still carrying BossKey = \"vanilla:king_slime\""
    - "Using the BossCoreItem in the main world sets NPC.downedSlimeKing = true via SetEventFlagCleared, confirmed by a visible flag-state change (e.g. Lantern Night eligibility / a tracker check)"
    - "Re-using a second BossCoreItem after the first successful apply is a no-op -- distinct chat feedback, no duplicate side effects, item still consumed"
    - "The full pipeline (subworld kill -> item drop -> main-world apply) works end-to-end against a backed-up world save"
  artifacts: []
  key_links: []
---

<objective>
Empirically prove, via a real in-game test, that the BossRegistry/BossCoreItem/GlobalNPC pipeline built in Plans 03-01/03-02 satisfies all 5 of this phase's Success Criteria and the DROP-02/DROP-03/APPLY-01/APPLY-04 requirements end-to-end, using King Slime as the one worked example.

Purpose: This project has no automated test framework for in-game tModLoader behavior (per 03-VALIDATION.md) -- a live playthrough is the only way to confirm the drop-rule gating, cross-world instance-data survival, vanilla flag-fidelity, and idempotency logic actually work together as designed, not just individually compile.
Output: A completed manual test run with confirmed results for every phase Success Criterion, recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bossregistry-bosscoreitem-globalnpc-pipeline-proof-of-concept/03-CONTEXT.md
@.planning/phases/03-bossregistry-bosscoreitem-globalnpc-pipeline-proof-of-concept/03-RESEARCH.md
@.planning/phases/03-bossregistry-bosscoreitem-globalnpc-pipeline-proof-of-concept/03-VALIDATION.md
@docs/WORLD_BACKUP_GUIDANCE.md
@Systems/BossRegistry.cs
@Items/BossCoreItem.cs
@ItemDropRules/BossCoreDropRule.cs
@GlobalNPCs/BossKillGlobalNPC.cs
@Tiles/Test1Tile.cs
@Items/Test1Item.cs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the live BossRegistry/BossCoreItem/GlobalNPC pipeline test</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Systems/BossRegistry.cs, Items/BossCoreItem.cs (Plan 03-01 -- registry + carrier item being exercised)
    - ItemDropRules/BossCoreDropRule.cs, GlobalNPCs/BossKillGlobalNPC.cs (Plan 03-02 -- drop-side wiring being exercised)
    - docs/WORLD_BACKUP_GUIDANCE.md (VERIFY-02 backup procedure -- required before this test)
    - .planning/phases/03-bossregistry-bosscoreitem-globalnpc-pipeline-proof-of-concept/03-CONTEXT.md (D-01 idempotency, D-02 consumption policy, D-04 flag fidelity -- what each step below is proving)
  </read_first>
  <action>Back up the world save per docs/WORLD_BACKUP_GUIDANCE.md, then run the full pipeline test in-game following the exact steps in how-to-verify below, and report the result of each numbered step.</action>
  <what-built>
    Plans 03-01/03-02 built the complete BossRegistry/BossCoreItem/GlobalNPC pipeline: a central key -> boss-definition registry with idempotent apply logic (Systems/BossRegistry.cs), a carrier item that survives the subworld round-trip and applies a boss's downed state on use (Items/BossCoreItem.cs), a custom drop rule that gates the drop dynamically to only fire inside the subworld and tags the spawned item with the correct key (ItemDropRules/BossCoreDropRule.cs), and the GlobalNPC hook that attaches that drop rule to every registered boss NPC type (GlobalNPCs/BossKillGlobalNPC.cs). This task verifies the full chain works together live and proves DROP-01 through APPLY-04 (this phase's Core Value: subworld kill -> carrier item -> main-world apply, exactly once).
  </what-built>
  <how-to-verify>
    Step 0 (VERIFY-02, mandatory before any live test against a real save): Follow docs/WORLD_BACKUP_GUIDANCE.md's Backup Procedure -- copy the .wld/.bak and .plr files to a timestamped backup folder and confirm the copy is readable (non-zero size) before proceeding.

    In singleplayer, with a Slime Crown and a placed Test1 portal tile available (craft Slime Crown from 1 Gel + 99 Gold Coins at any Anvil if you don't already have one; place a Test1 tile if none is placed from Phase 2's test):

    1. Negative check (DROP-02): In the main world (NOT inside the subworld), use the Slime Crown directly (not via the Test1 tile) to summon King Slime normally, and kill it. Confirm NO BossCoreItem drops -- only King Slime's normal vanilla loot.
    2. Redirect + kill (DROP-02 positive): Right-click the Test1 tile while holding the Slime Crown to redirect into the boss-arena subworld (per Phase 2's confirmed SUBW-01..04 flow). Once King Slime auto-spawns, kill it. Confirm a BossCoreItem drops (placeholder texture, matches Items/BossCoreItem.png).
    3. Pick up the dropped BossCoreItem. Confirm it appears in your inventory.
    4. Exit the subworld via SubworldLibrary's Return button (pause menu). Confirm you're back in the main world with the BossCoreItem still present in your inventory (DROP-03: BossKey survived the cross-world trip).
    5. Use the BossCoreItem (left-click while it's in your hotbar/selected). Confirm:
       - A chat message confirms success ("Boss credential applied: vanilla:king_slime" or equivalent, per D-02).
       - The item is consumed (no longer in inventory).
       - King Slime's downed state is now set in the main world -- verify via a Boss Checklist-equivalent tracker mod if installed, OR by observing Lantern Night eligibility change, OR by re-entering the subworld and confirming BossSummonPlayer/registry-adjacent behavior is unaffected (any one confirmation is sufficient; note which method you used).
    6. Idempotency check (APPLY-04): Repeat steps 1-2 to kill King Slime again inside the subworld and obtain a second BossCoreItem. Exit the subworld, then use the second BossCoreItem. Confirm:
       - A DIFFERENT chat message appears ("This boss was already marked defeated" or equivalent, per D-01/D-02) -- not the success message from step 5.
       - The item IS still consumed (per D-01: AlreadyDowned is a no-op, not an error -- still consume).
       - No duplicate/erroneous side effects occur (no crash, no double achievement popup, no error message).
  </how-to-verify>
  <verify>Step 1 confirms the subworld gate is dynamic (no drop outside); step 2-4 confirm drop + cross-world instance-data survival; step 5 confirms APPLY-01; step 6 confirms APPLY-04 idempotency</verify>
  <acceptance_criteria>
    - Step 0 backup completed and confirmed readable before any other step
    - Step 1 confirms no BossCoreItem drops from a main-world King Slime kill
    - Step 2 confirms a BossCoreItem drops from a subworld King Slime kill
    - Step 3-4 confirm the item survives pickup and the subworld exit trip
    - Step 5 confirms UseItem applies the flag, shows a success message, and consumes the item
    - Step 6 confirms a second use after the flag is already set shows different (no-op) feedback, still consumes the item, and produces no duplicate side effects
  </acceptance_criteria>
  <done>All 5 phase Success Criteria and DROP-02/DROP-03/APPLY-01/APPLY-04 are empirically confirmed working end-to-end via a real King Slime kill/carry/apply cycle, against a backed-up world save.</done>
  <resume-signal>Report the result of each numbered step above. Type "pipeline verified" if all steps passed as expected, or describe exactly what went wrong (which step, what was observed instead) otherwise.</resume-signal>
</task>

</tasks>

<verification>
This plan's entire verification is Task 1's live checkpoint -- no automated test framework exists for in-game tModLoader behavior (per 03-VALIDATION.md). No files are modified by this plan; it is a pure verification gate closing out Phase 3.
</verification>

<success_criteria>
- Live test confirms all 5 phase Success Criteria: registry-gated drop only inside the subworld, cross-world BossKey survival, correct vanilla-fidelity flag application via SetEventFlagCleared, idempotent re-use with no duplicate side effects, and the full pipeline demonstrated end-to-end against a backed-up save.
- Every requirement this phase owns (DROP-01, DROP-02, DROP-03, APPLY-01, APPLY-04) is confirmed satisfied, closing out Phase 3 per REQUIREMENTS.md's Traceability table.
</success_criteria>

<output>
After completion, create `.planning/phases/03-bossregistry-bosscoreitem-globalnpc-pipeline-proof-of-concept/03-03-SUMMARY.md`, recording the actual live test results for each of the 6 numbered steps above and the phase's 5 Success Criteria.
</output>
