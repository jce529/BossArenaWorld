---
phase: 07-noxusboss-continentofjourney-daybreak-integration
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: []
autonomous: false
requirements: [MOD-06]

must_haves:
  truths:
    - "Live in-game test confirms ContinentOfJourney.DownedBossSystem.downedGoblinChariot == true after using the carrier item in the main world following a real Goblin Chariot kill in the subworld, and Homeward Journey's own bundled CoJ_BossChecklist.cs integration correctly surfaces Goblin Chariot as downed in Boss Checklist's tracker UI (closes 07-RESEARCH.md Open Question 2)"
    - "Re-using the carrier item does not duplicate any side effect (APPLY-04 idempotency)"
    - "The mod continues to load and run safely with ContinentOfJourney (Homeward Journey) disabled -- no JITException naming Integrations/HomewardJourneyIntegration.cs in Logs/client.log"
  artifacts: []
  key_links: []
---

<objective>
Empirically verify, via live in-game checkpoints on a backed-up throwaway world, that Plan 01's `Integrations/HomewardJourneyIntegration.cs` actually works end-to-end at runtime -- not just that it compiles. Covers Phase 7's Success Criteria 1-2: Goblin Chariot's downed-flag application (including Boss Checklist recognition) and ContinentOfJourney-disabled load safety.

Purpose: Closes MOD-06's live-verification gap (07-VALIDATION.md: "manual-only, dedicated throwaway world" -- no automated tModLoader in-game test harness exists in this project, per Phase 1-6/9 precedent) and Phase 7's Success Criteria 1-2. This is the fifth and final v1 mod integration -- once this checkpoint passes, ROADMAP.md Phase 7's Goal ("completing v1 mod coverage") is fully satisfied. ContinentOfJourney is not currently installed/enabled in the local `Mods\` folder (07-RESEARCH.md Environment Availability) -- Task 1 covers getting it enabled as its first step.
Output: Two completed manual checkpoint results (Goblin-Chariot-downed-applies + Boss Checklist recognition confirmed; ContinentOfJourney-disabled safety confirmed), recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-noxusboss-continentofjourney-daybreak-integration/07-CONTEXT.md
@.planning/phases/07-noxusboss-continentofjourney-daybreak-integration/07-RESEARCH.md
@.planning/phases/07-noxusboss-continentofjourney-daybreak-integration/07-VALIDATION.md
@docs/WORLD_BACKUP_GUIDANCE.md
@Integrations/HomewardJourneyIntegration.cs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live Goblin Chariot downed-flag application + Boss Checklist recognition checkpoint</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/HomewardJourneyIntegration.cs (RegisterGoblinChariot/ApplyGoblinChariotDowned/IsGoblinChariotDowned -- the exact flag write this checkpoint confirms replays correctly)
    - docs/WORLD_BACKUP_GUIDANCE.md (backup procedure -- follow before testing on a real/dedicated throwaway world, per VERIFY-02)
    - .planning/phases/07-noxusboss-continentofjourney-daybreak-integration/07-RESEARCH.md "Environment Availability" (ContinentOfJourney is not currently enabled in the local Mods folder -- confirmed missing dependency with a low-effort fallback) and "Open Question 2" (Homeward Journey ships its own CoJ_BossChecklist.cs BossChecklist integration -- this checkpoint is the empirical confirmation that Open Question 2 asked for)
  </read_first>
  <action>
    Part A -- Enable the mod:
    1. If not already subscribed, subscribe to Homeward Journey (Steam Workshop id 2930931197,
       https://steamcommunity.com/sharedfiles/filedetails/?id=2930931197) via the in-game Mod
       Browser or Steam Workshop page, then launch tModLoader once so it syncs the Workshop cache
       into the local `Mods\` folder.
    2. In Mod Configuration, enable ContinentOfJourney (displayed as "Homeward Journey") alongside
       the existing CalamityMod, SpiritMod, Redemption, CatalystMod, SubworldLibrary, CheatSheet,
       BossChecklist, and BossArenaSubWorld, then reload mods.
    3. Confirm mods load with no error (this alone is not the disabled-safety checkpoint -- that
       is Task 2 below).

    Part B -- World backup:
    4. Follow docs/WORLD_BACKUP_GUIDANCE.md and back up (or create a fresh dedicated throwaway)
       world before testing.

    Part C -- Goblin Chariot:
    5. Acquire a `PurpleFlareGun` item (e.g. via CheatSheet's item-spawn menu, already installed).
    6. Right-click the Test1 portal tile while holding `PurpleFlareGun` to redirect into the
       boss-arena subworld (per SUBW-02/SUBW-03); confirm Goblin Chariot auto-summons on arrival
       (SUBW-04).
    7. Kill Goblin Chariot; confirm a `BossCoreItem` tagged `continentofjourney:goblin_chariot`
       drops (only inside the subworld, per DROP-02).
    8. Return to the main world using SubworldLibrary's Return mechanism.
    9. Use the dropped `BossCoreItem`; confirm:
       - `ContinentOfJourney.DownedBossSystem.downedGoblinChariot` becomes `true` (check via Boss
         Checklist's tracker UI, or CheatSheet's variable inspector if available)
       - Boss Checklist's tracker UI shows Goblin Chariot as downed/checked off, confirming
         Homeward Journey's own bundled `CoJ_BossChecklist.cs` integration recognizes the flag
         with no extra work from this project (closes 07-RESEARCH.md Open Question 2, and doubles
         as this phase's contribution to Phase 8's VERIFY-03)
       - No JITException or runtime exception occurs during `NPC.SetEventFlagCleared`
    10. Re-use the same carrier-item path and confirm no double-apply / no duplicate Boss Checklist
        entry (APPLY-04 idempotency).
  </action>
  <what-built>
    Plan 01 registered Goblin Chariot (`continentofjourney:goblin_chariot`) into the existing
    BossRegistry/SummonItemRegistry pipeline, using ContinentOfJourney's actual public-static-field
    downed-tracking API confirmed via decompiled source. This task verifies the full
    subworld-kill -> carrier-item-drop -> main-world-apply pipeline actually works at runtime,
    including Boss Checklist's own external recognition of the flag, not just that the code
    compiles.
  </what-built>
  <how-to-verify>
    Follow the 10 numbered steps in the action above (Parts A-C in order).
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader boss-kill/carrier-item behavior (per 07-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - ContinentOfJourney (Homeward Journey) subscribed, enabled, and loaded without error
    - World backed up per docs/WORLD_BACKUP_GUIDANCE.md before any live test
    - Goblin Chariot killed in the subworld, BossCoreItem tagged continentofjourney:goblin_chariot dropped and used in the main world, DownedBossSystem.downedGoblinChariot confirmed true
    - Boss Checklist's tracker UI confirms Goblin Chariot recognized as downed after the carrier item is used
    - Re-using the carrier item does not duplicate any side effect or Boss Checklist entry (APPLY-04)
  </acceptance_criteria>
  <done>Goblin Chariot's full subworld-kill-to-main-world-apply pipeline is empirically confirmed working, including Boss Checklist recognition, satisfying Phase 7 Success Criterion 1.</done>
  <resume-signal>Type "goblin chariot verified" if all 10 steps passed, or describe exactly which step failed and what was observed instead.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: ContinentOfJourney-disabled load-safety checkpoint</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/HomewardJourneyIntegration.cs (confirm every method touching a ContinentOfJourney type carries its own [JITWhenModsEnabled("ContinentOfJourney")] tag -- the property this checkpoint is actually testing at runtime)
    - .planning/phases/06-redemption-catalystmod-integration/06-03-PLAN.md Task 2 (the established precedent for this exact style of checkpoint -- same failure mode, a different integration file)
    - .planning/PROJECT.md Key Decisions (the Phase 4 lambda/[JITWhenModsEnabled] JIT-crash lesson, commit 0e19600, and the Phase 9 lesson that lazy construction alone is not sufficient JIT protection)
  </read_first>
  <action>
    1. In tModLoader's Mod Configuration menu, disable ContinentOfJourney (Homeward Journey) only
       (leave CalamityMod, SpiritMod, Redemption, CatalystMod, SubworldLibrary, and
       BossArenaSubWorld enabled), then relaunch tModLoader and load into any world.
    2. Confirm tModLoader loads to the main menu and into the world without a JIT crash, exception
       dialog, or mod-load error mentioning "ContinentOfJourney" or "Homeward Journey".
    3. Check `Logs/client.log` for any exception naming `HomewardJourneyIntegration` -- confirm
       none are present.
    4. Confirm BossArenaSubWorld's other, ContinentOfJourney-independent features (King Slime,
       Hive Mind, Infernon, Thorn, Astrageldon) still function normally.
    5. Re-enable ContinentOfJourney, restoring the normal mod list for future phases.
  </action>
  <what-built>
    Plan 01's integration file isolates every ContinentOfJourney type reference behind a named,
    separately-`[JITWhenModsEnabled("ContinentOfJourney")]`-tagged method, following the locked
    project-wide discipline established in Phase 4 (a real live JITException from an inline
    lambda) and reinforced in Phase 9 (lazy construction alone is not sufficient). This task
    verifies that isolation boundary actually prevents a JIT crash at runtime when the mod is
    genuinely absent, not just that the code looks correctly isolated on review.
  </what-built>
  <how-to-verify>
    Follow the 5 numbered steps in the action above.
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader mod-load JIT behavior (per 07-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - tModLoader launched and loaded into a world successfully with ContinentOfJourney disabled, no JIT crash or exception dialog observed
    - Logs/client.log contains no ContinentOfJourney-related exception naming HomewardJourneyIntegration
    - Other registered bosses' features (King Slime, Hive Mind, Infernon, Thorn, Astrageldon) still function normally
    - ContinentOfJourney re-enabled afterward, mod list confirmed restored to normal
  </acceptance_criteria>
  <done>The integration is empirically confirmed to load and run safely at runtime with ContinentOfJourney disabled, satisfying Phase 7 Success Criterion 2 and completing MOD-06/v1 mod coverage.</done>
  <resume-signal>Type "mod-disabled safety verified" if the mod loaded cleanly with no relevant exceptions in the log, or paste the exact exception/error otherwise.</resume-signal>
</task>

</tasks>

<verification>
Both checkpoint tasks in this plan have no automated equivalent (per 07-VALIDATION.md -- no in-game test harness exists in this project). Verification is entirely the human-confirmed resume-signals: "goblin chariot verified" (Task 1) and "mod-disabled safety verified" (Task 2).
</verification>

<success_criteria>
- Live test confirms Goblin Chariot's downed flag applies correctly in the main world and is recognized by Boss Checklist (Phase 7 SC1)
- Live test confirms the mod loads and runs safely with ContinentOfJourney disabled, no JIT crash (Phase 7 SC2)
- MOD-06 fully satisfied end-to-end (research + registration + live verification) -- v1 mod coverage complete
</success_criteria>

<output>
After completion, create `.planning/phases/07-noxusboss-continentofjourney-daybreak-integration/07-02-SUMMARY.md`, recording the actual live test results for both checkpoints.
</output>
