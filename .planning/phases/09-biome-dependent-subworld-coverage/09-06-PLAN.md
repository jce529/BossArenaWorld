---
phase: 09-biome-dependent-subworld-coverage
plan: 06
type: execute
wave: 3
depends_on: ["05"]
files_modified: []
autonomous: false
requirements: [ARENA-01]

must_haves:
  truths:
    - "Each of the 9 new biome subworlds (Hallow, Underworld, Jungle, Space, Desert, Dungeon, Astral, Sulphurous, Briar) is confirmed to generate without crashing and spawn the player above the platform surface, via the temporary debug enter command"
    - "Each of the 9 new biome subworlds is confirmed to actually make its target Zone/Biome flag read true while the player stands on the platform -- not just 'looks right', per the temporary debug flag-check command"
    - "Dungeon specifically is confirmed to satisfy BOTH the weighted tile-count AND the wall-at-player's-tile requirement (the one extra check no other biome in this project has), not just the tile count alone"
  artifacts: []
  key_links: []
---

<objective>
Empirically prove, via three live in-game checkpoints (one per mechanism family, grouped per 09-VALIDATION.md's sampling guidance), that all 9 biome `Subworld`/`GenPass` pairs built in Plans 01-04 actually satisfy their target Zone/Biome flag when entered -- not just that they compile and structurally mirror the Corruption precedent. This project has no automated test framework for in-game tModLoader biome detection (per 09-VALIDATION.md); the entire correctness claim of "does the fake arena make vanilla/Calamity/Spirit's real per-tick biome detection return true" can only be confirmed by standing on the platform and reading the flag live.

Purpose: Closes ARENA-01's Success Criterion 2 ("every boss classified as biome/Zone-dependent has a matching routed biome-variant subworld") for the infrastructure half of that claim -- the arena genuinely satisfies its target biome, ready for Phase 6/7 to route real bosses into.
Output: Three completed manual checkpoint results (vanilla tile-weighted biomes confirmed; height-only biomes confirmed; modded ModBiome biomes confirmed), recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-biome-dependent-subworld-coverage/09-CONTEXT.md
@.planning/phases/09-biome-dependent-subworld-coverage/09-RESEARCH.md
@.planning/phases/09-biome-dependent-subworld-coverage/09-VALIDATION.md
@docs/WORLD_BACKUP_GUIDANCE.md
@Debug/BiomeArenaDebugCommands.cs
@Subworlds/BossArenaCorruptionSubworld.cs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live verification -- vanilla tile-weighted biomes (Hallow, Jungle, Desert, Dungeon)</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Subworlds/HallowPlatformPass.cs, Subworlds/JunglePlatformPass.cs, Subworlds/DesertPlatformPass.cs, Subworlds/DungeonPlatformPass.cs (Plans 02-03's exact tile/threshold values being exercised)
    - Debug/BiomeArenaDebugCommands.cs (the bossarena-enterbiome and bossarena-checkbiomeflags commands used to run this checkpoint)
    - .planning/phases/09-biome-dependent-subworld-coverage/09-RESEARCH.md "Pitfall 3: Dungeon needs a WALL, not just tiles" (Dungeon's extra check -- the one most likely to fail if ApplyPass's wall-placement line was dropped or misplaced)
  </read_first>
  <action>
    Use a throwaway/test world (or the existing backed-up main save per `docs/WORLD_BACKUP_GUIDANCE.md` --
    entering these subworlds does not permanently alter the main world's own terrain, only the
    disposable arena subworld's `ShouldSave=false` file) with SubworldLibrary and BossArenaSubWorld
    enabled. For EACH of the 4 biomes below, in turn:

    1. Run `/bossarena-enterbiome <name>` (hallow, jungle, desert, dungeon in turn).
    2. Confirm the subworld generates without a crash/exception and the player spawns standing on
       (not inside/below) the platform surface.
    3. Run `/bossarena-checkbiomeflags` and confirm the matching flag reads `True`:
       - hallow -> `ZoneHallow=True`
       - jungle -> `ZoneJungle=True`
       - desert -> `ZoneDesert=True`
       - dungeon -> `ZoneDungeon=True` (the only one of these 4 with an extra wall requirement --
         if this reads `False` despite `DungeonTileCount` looking correct, suspect the wall-placement
         line in `DungeonPlatformPass.cs`, per Pitfall 3)
    4. Return to the main/test world via SubworldLibrary's Return button before moving to the next biome.
  </action>
  <what-built>
    Plans 02-03 built `Subworlds/BossArenaHallowSubworld.cs`/`HallowPlatformPass.cs`,
    `BossArenaJungleSubworld.cs`/`JunglePlatformPass.cs`, `BossArenaDesertSubworld.cs`/`DesertPlatformPass.cs`,
    and `BossArenaDungeonSubworld.cs`/`DungeonPlatformPass.cs` -- four vanilla tile-weighted biome arenas.
    This task verifies each one's real per-tick Zone-flag detection actually returns true on entry, not
    just that the decompiled threshold math checks out on paper.
  </what-built>
  <how-to-verify>
    Follow the 4 numbered steps in the action above, once per biome (4 total passes).
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader biome-flag detection (per 09-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - All 4 subworlds (hallow/jungle/desert/dungeon) generated without a crash and the player spawned above the platform surface
    - `ZoneHallow=True` while standing on the hallow platform
    - `ZoneJungle=True` while standing on the jungle platform
    - `ZoneDesert=True` while standing on the desert platform
    - `ZoneDungeon=True` while standing on the dungeon platform (confirms both the tile-count AND wall requirement are satisfied)
  </acceptance_criteria>
  <done>All 4 vanilla tile-weighted biome subworlds are empirically confirmed to generate correctly and satisfy their target Zone flag on entry.</done>
  <resume-signal>Type "vanilla tile-weighted biomes verified" if all 4 flags read True as expected, or describe exactly which biome/flag failed and what was observed instead.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification -- height-only biomes (Underworld, Space)</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Subworlds/UnderworldPlatformPass.cs, Subworlds/SpacePlatformPass.cs (Plan 01's exact surfaceY values being exercised: 650 and 50)
    - Debug/BiomeArenaDebugCommands.cs (the bossarena-enterbiome and bossarena-checkbiomeflags commands used to run this checkpoint)
    - .planning/phases/09-biome-dependent-subworld-coverage/09-RESEARCH.md "Pitfall 1: Assuming every biome needs a themed tile fill" (the reason this checkpoint is testing PLATFORM POSITION, not tile composition -- if these flags read False, suspect surfaceY first, not tile type)
  </read_first>
  <action>
    Using the same world as Task 1, for EACH of the 2 biomes below, in turn:

    1. Run `/bossarena-enterbiome <name>` (underworld, space in turn).
    2. Confirm the subworld generates without a crash/exception and the player spawns standing on
       (not inside/below) the platform surface.
    3. Run `/bossarena-checkbiomeflags` and confirm the matching flag reads `True`:
       - underworld -> `ZoneUnderworldHeight=True`
       - space -> `ZoneSkyHeight=True`
    4. Return to the main/test world via SubworldLibrary's Return button before moving to the next biome.
  </action>
  <what-built>
    Plan 01 built `Subworlds/BossArenaUnderworldSubworld.cs`/`UnderworldPlatformPass.cs` and
    `BossArenaSpaceSubworld.cs`/`SpacePlatformPass.cs` -- two height-only biome arenas where platform
    Y-position alone determines the Zone flag, with no tile-composition requirement at all. This task
    verifies the chosen surfaceY values (650 and 50) actually land on the correct side of each flag's
    real boundary, not just that the decompiled formula checks out on paper.
  </what-built>
  <how-to-verify>
    Follow the 4 numbered steps in the action above, once per biome (2 total passes).
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader biome-flag detection (per 09-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - Both subworlds (underworld/space) generated without a crash and the player spawned above the platform surface
    - `ZoneUnderworldHeight=True` while standing on the underworld platform
    - `ZoneSkyHeight=True` while standing on the space platform
  </acceptance_criteria>
  <done>Both height-only biome subworlds are empirically confirmed to generate correctly and satisfy their target Zone flag on entry, purely from platform position.</done>
  <resume-signal>Type "height-only biomes verified" if both flags read True as expected, or describe exactly which biome/flag failed and what was observed instead.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verification -- modded ModBiome biomes (Astral, Sulphurous, Briar)</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Subworlds/AstralPlatformPass.cs, Subworlds/SulphurousPlatformPass.cs, Subworlds/BriarPlatformPass.cs (Plan 04's exact tile/threshold values being exercised)
    - Debug/BiomeArenaDebugCommands.cs (the bossarena-enterbiome and bossarena-checkbiomeflags commands used to run this checkpoint -- note the Calamity/Spirit flag print lines only run if the respective mod is installed)
    - .planning/phases/09-biome-dependent-subworld-coverage/09-RESEARCH.md per-biome table rows "Astral Infection"/"Sulphurous Sea"/"Briar" (the modern ModBiome mechanism, structurally different from Task 1's vanilla SceneMetrics mechanism)
  </read_first>
  <action>
    Using a world with SubworldLibrary, BossArenaSubWorld, CalamityMod, AND SpiritMod all enabled
    (required for this checkpoint specifically -- Task 1/2 did not need Calamity/Spirit installed),
    for EACH of the 3 biomes below, in turn:

    1. Run `/bossarena-enterbiome <name>` (astral, sulphur, briar in turn).
    2. Confirm the subworld generates without a crash/exception and the player spawns standing on
       (not inside/below) the platform surface.
    3. Run `/bossarena-checkbiomeflags` and confirm the matching flag reads `True`:
       - astral -> `ZoneAstral=True` (also confirm the vanilla line shows `ZoneDungeon=False`, since
         Astral Infection's IsBiomeActive requires `!player.ZoneDungeon` as an AND-condition)
       - sulphur -> `ZoneSulphur=True`
       - briar -> `Briar.InBriar=True`
    4. Return to the main/test world via SubworldLibrary's Return button before moving to the next biome.
  </action>
  <what-built>
    Plan 04 built `Subworlds/BossArenaAstralSubworld.cs`/`AstralPlatformPass.cs`,
    `BossArenaSulphurousSubworld.cs`/`SulphurousPlatformPass.cs`, and
    `BossArenaBriarSubworld.cs`/`BriarPlatformPass.cs` -- three modded `ModBiome`-based arenas using
    Calamity/Spirit's own tile-count-hook mechanism, structurally different from Task 1's vanilla
    `SceneMetrics` mechanism. This task verifies each biome's real `ModBiome.IsBiomeActive()` check
    actually returns true on entry, not just that the decompiled threshold math checks out on paper.
  </what-built>
  <how-to-verify>
    Follow the 4 numbered steps in the action above, once per biome (3 total passes).
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader modded-biome-flag detection (per 09-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - All 3 subworlds (astral/sulphur/briar) generated without a crash and the player spawned above the platform surface
    - `ZoneAstral=True` AND `ZoneDungeon=False` while standing on the astral platform
    - `ZoneSulphur=True` while standing on the sulphur platform
    - `Briar.InBriar=True` while standing on the briar platform
  </acceptance_criteria>
  <done>All 3 modded ModBiome biome subworlds are empirically confirmed to generate correctly and satisfy their target Zone/Biome flag on entry, with CalamityMod and SpiritMod both installed.</done>
  <resume-signal>Type "modded biomes verified" if all 3 flags read True (and ZoneDungeon=False for astral) as expected, or describe exactly which biome/flag failed and what was observed instead.</resume-signal>
</task>

</tasks>

<verification>
This plan's entire verification is its three live checkpoints -- no automated test framework exists for in-game tModLoader biome-flag detection (per 09-VALIDATION.md). No repo files are modified by this plan; it is a pure verification gate over Plans 01-05's output. Plan 07 (Wave 4) covers the remaining CalamityMod/SpiritMod-disabled load-safety checkpoint and the debug-hook cleanup, separately.
</verification>

<success_criteria>
- Live test confirms all 4 vanilla tile-weighted biomes (Hallow, Jungle, Desert, Dungeon) generate correctly and their target Zone flag reads true on entry, including Dungeon's extra wall requirement
- Live test confirms both height-only biomes (Underworld, Space) generate correctly and their target Zone flag reads true purely from platform position
- Live test confirms all 3 modded ModBiome biomes (Astral, Sulphurous, Briar) generate correctly and their target Zone/Biome flag reads true on entry, with CalamityMod and SpiritMod both installed
- ARENA-01's Success Criterion 2 is empirically confirmed for the arena-construction half of the claim (real biome tiles genuinely satisfy their target flag) -- ready for Phase 6/7 to route real bosses into these 9 subworlds
</success_criteria>

<output>
After completion, create `.planning/phases/09-biome-dependent-subworld-coverage/09-06-SUMMARY.md`, recording the actual live test results for all three checkpoints.
</output>
