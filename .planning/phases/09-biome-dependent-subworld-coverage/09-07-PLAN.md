---
phase: 09-biome-dependent-subworld-coverage
plan: 07
type: execute
wave: 4
depends_on: ["06"]
files_modified:
  - Debug/BiomeArenaDebugCommands.cs
autonomous: false
requirements: [ARENA-01]

must_haves:
  truths:
    - "The mod continues to load and run safely with CalamityMod disabled -- no JITException naming BossArenaAstralSubworld/AstralPlatformPass, confirming 09-RESEARCH.md Pitfall 4's JIT-safety discipline actually holds at runtime, not just in code review. (Sulphurous Sea was descoped 2026-08-14, D-07 -- not part of this checkpoint)"
    - "The mod continues to load and run safely with SpiritMod disabled -- no JITException naming BossArenaBriarSubworld/BriarPlatformPass, same discipline applied to the Spirit-referencing pair"
    - "Debug/BiomeArenaDebugCommands.cs is deleted once both safety checkpoints pass, since D-02 forbids any new PERMANENT player-facing entry point and this file was explicitly temporary (Plan 05)"
    - "The project still builds successfully (dotnet build exit 0) after the debug file's removal, with all 7 biome Subworld/GenPass pairs from Plans 01-04 still present and intact"
  artifacts: []
  key_links: []
---

<objective>
Empirically prove, via two live in-game checkpoints, that the 2 modded-biome `Subworld`/`GenPass` pairs (Astral, Briar) built in Plan 04 satisfy 09-RESEARCH.md Pitfall 4's JIT-safety discipline at runtime -- the mod must still load and run correctly with CalamityMod and SpiritMod each independently disabled, mirroring Phase 4/5's established D-05 checkpoint pattern. (Sulphurous Sea was descoped from Phase 9 by user decision 2026-08-14, D-07 in 09-CONTEXT.md, after its code had already been built; that code was discarded before merging to master and is not part of this checkpoint.) Then remove the temporary debug entry mechanism (Plan 05) now that all live verification is complete, restoring D-02's "no new permanent player-facing entry point" constraint -- mirroring Phase 2's precedent of combining a final live checkpoint with debug-tooling removal in one plan (02-03-PLAN.md).

Purpose: Closes out ARENA-01's remaining infrastructure risk (a JIT crash from a leaked Calamity/Spirit type reference outside the `Tasks`/`ApplyPass()` lazy boundary would only surface with the mod actually disabled, not via `dotnet build` alone) and returns the codebase to its D-02-compliant state before this phase closes.
Output: Two completed manual checkpoint results (CalamityMod-disabled safety confirmed; SpiritMod-disabled safety confirmed) plus the deletion of `Debug/BiomeArenaDebugCommands.cs`, recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-biome-dependent-subworld-coverage/09-CONTEXT.md
@.planning/phases/09-biome-dependent-subworld-coverage/09-RESEARCH.md
@.planning/phases/09-biome-dependent-subworld-coverage/09-VALIDATION.md
@Debug/BiomeArenaDebugCommands.cs
@Subworlds/BossArenaAstralSubworld.cs
@Subworlds/BossArenaBriarSubworld.cs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live CalamityMod-disabled and SpiritMod-disabled safety checkpoint</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Subworlds/BossArenaAstralSubworld.cs, Subworlds/BossArenaBriarSubworld.cs (confirm none contain a direct CalamityMod/SpiritMod type reference -- the property this checkpoint is actually testing at runtime)
    - Subworlds/AstralPlatformPass.cs, Subworlds/BriarPlatformPass.cs (the classes that DO reference Calamity/Spirit types, confined to their own ApplyPass() bodies -- these are never touched unless their paired Subworld is actually entered)
    - .planning/phases/09-biome-dependent-subworld-coverage/09-RESEARCH.md "Pitfall 4: Leaking a Calamity/Spirit type reference outside the Tasks/ApplyPass() lazy boundary" (the exact failure mode this checkpoint is designed to catch)
    - Integrations/CalamityIntegration.cs, Integrations/SpiritIntegration.cs (Phase 4/5's precedent for this exact style of checkpoint -- same failure mode, different call-site)
    - .planning/phases/09-biome-dependent-subworld-coverage/09-CONTEXT.md D-07 (Sulphurous Sea was descoped 2026-08-14 -- its Subworld/GenPass pair was built then discarded, not merged; this checkpoint covers Astral and Briar only)
  </read_first>
  <action>
    Part A -- CalamityMod disabled:
    1. In tModLoader's Mod Configuration menu, disable CalamityMod (leave SpiritMod, SubworldLibrary,
       and BossArenaSubWorld in whatever state they were previously), then relaunch tModLoader and
       load into any world.
    2. Confirm tModLoader loads to the main menu and into the world without a JIT crash, exception
       dialog, or mod-load error mentioning "CalamityMod".
    3. Check `Logs/client.log` for any exception naming `BossArenaAstralSubworld` or `AstralPlatformPass`
       -- confirm none are present.
    4. Confirm BossArenaSubWorld's other, CalamityMod-independent features (Phase 1-3's King Slime
       pipeline, Phase 5's Infernon if SpiritMod is enabled) still function normally.
    5. Re-enable CalamityMod.

    Part B -- SpiritMod disabled:
    6. In tModLoader's Mod Configuration menu, disable SpiritMod (leave CalamityMod, SubworldLibrary,
       and BossArenaSubWorld in whatever state they were previously), then relaunch tModLoader and
       load into any world.
    7. Confirm tModLoader loads to the main menu and into the world without a JIT crash, exception
       dialog, or mod-load error mentioning "SpiritMod".
    8. Check `Logs/client.log` for any exception naming `BossArenaBriarSubworld` or `BriarPlatformPass`
       -- confirm none are present.
    9. Confirm BossArenaSubWorld's other, SpiritMod-independent features still function normally.
    10. Re-enable SpiritMod, restoring the normal mod list for future phases.
  </action>
  <what-built>
    Plan 04 built 2 modded-biome `Subworld`/`GenPass` pairs (Astral, Briar -- Sulphurous Sea was
    descoped by user decision 2026-08-14, D-07), each structured so the paired
    `BossArenaXSubworld.cs` file contains zero direct Calamity/Spirit type references (confined
    exclusively to the paired `XPlatformPass.cs` file, per 09-RESEARCH.md Pitfall 4). This task
    verifies that isolation boundary actually prevents a JIT crash at runtime when the respective
    mod is genuinely absent, not just that the code looks correctly isolated on review -- the same
    style of checkpoint Phase 4/5 ran for their own `Integrations/*.cs` JIT boundaries, applied here
    to a structurally different call site (`Subworld.Tasks` getter instead of a `PostSetupContent()`
    guard).
  </what-built>
  <how-to-verify>
    Follow the 10 numbered steps in the action above (Part A, then Part B).
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader mod-load JIT behavior (per 09-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - tModLoader launched and loaded into a world successfully with CalamityMod disabled, no JIT crash or exception dialog observed
    - Logs/client.log contains no CalamityMod-related exception naming BossArenaAstralSubworld/AstralPlatformPass
    - tModLoader launched and loaded into a world successfully with SpiritMod disabled, no JIT crash or exception dialog observed
    - Logs/client.log contains no SpiritMod-related exception naming BossArenaBriarSubworld/BriarPlatformPass
    - Both mods re-enabled afterward, mod list confirmed restored to normal
  </acceptance_criteria>
  <done>ARENA-01's JIT-safety discipline (09-RESEARCH.md Pitfall 4) is empirically confirmed to hold at runtime for both modded-biome Subworld/GenPass pairs, with both CalamityMod and SpiritMod independently disabled.</done>
  <resume-signal>Type "mod-disabled safety verified" if both parts loaded cleanly with no relevant exceptions in the log, or paste the exact exception/error otherwise.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Remove the temporary debug entry mechanism</name>
  <files>Debug/BiomeArenaDebugCommands.cs (deleted)</files>
  <read_first>
    - Debug/BiomeArenaDebugCommands.cs (the file being deleted -- confirm Task 1's checkpoint above has passed before deleting, since this file is the only way to re-run any of Plan 06/07's live checkpoints)
    - 09-CONTEXT.md D-02 (no new PERMANENT player-facing entry point -- this temporary file's removal is what restores full compliance with that decision)
  </read_first>
  <action>
    Only perform this task after Task 1's checkpoint has been confirmed passing (resume-signal
    "mod-disabled safety verified" received) AND Plan 06's three checkpoints have all passed.

    Delete `Debug/BiomeArenaDebugCommands.cs` entirely:
    ```
    cd "C:\Users\chang\Documents\My Games\Terraria\tModLoader\ModSources\BossArenaSubWorld"
    rm Debug/BiomeArenaDebugCommands.cs
    ```

    Then run:
    ```
    dotnet build BossArenaSubWorld.csproj
    ```
    The build must still succeed (exit code 0) -- confirms no other file in the project depends on
    the deleted debug commands, and all 7 biome `Subworld`/`GenPass` pairs from Plans 01-04 remain
    intact and unaffected by the removal.
  </action>
  <verify>
    <automated>cd "C:\Users\chang\Documents\My Games\Terraria\tModLoader\ModSources\BossArenaSubWorld" && dotnet build BossArenaSubWorld.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `Debug/BiomeArenaDebugCommands.cs` no longer exists on disk
    - `dotnet build BossArenaSubWorld.csproj` exits with code 0 after the deletion
    - All 14 files from Plans 01-04 (`Subworlds/BossArenaHallowSubworld.cs` through `Subworlds/BriarPlatformPass.cs`, excluding Dungeon/Sulphurous per D-07) still exist, unaffected by this deletion
  </acceptance_criteria>
  <done>Debug/BiomeArenaDebugCommands.cs is deleted now that all Phase 9 live verification is complete, restoring D-02's "no new permanent player-facing entry point" constraint, and the project still builds successfully with all 7 biome Subworld/GenPass pairs intact.</done>
</task>

</tasks>

<verification>
Task 1's live checkpoint (CalamityMod-disabled + SpiritMod-disabled safety) has no automated equivalent (per 09-VALIDATION.md). Task 2's cleanup is verified via `dotnet build BossArenaSubWorld.csproj` exiting 0 after `Debug/BiomeArenaDebugCommands.cs`'s deletion, and confirming all 14 Plan 01-04 files remain present (`ls Subworlds/BossArena*.cs Subworlds/*PlatformPass.cs` should list exactly 18 files: the 7 new pairs (14 files) + the 2 pre-existing pairs from Phases 1/4 (4 files)).
</verification>

<success_criteria>
- Live test confirms the mod loads and runs safely with CalamityMod disabled, with no JIT crash and no relevant exceptions in the client log naming the Astral Subworld/GenPass classes
- Live test confirms the mod loads and runs safely with SpiritMod disabled, with no JIT crash and no relevant exceptions in the client log naming the Briar Subworld/GenPass classes
- `Debug/BiomeArenaDebugCommands.cs` is deleted, restoring D-02's "no new permanent player-facing entry point" constraint
- `dotnet build BossArenaSubWorld.csproj` succeeds with zero errors/warnings after the deletion
- ARENA-01 is confirmed satisfied end-to-end for the arena-construction/JIT-safety half of the claim (7 biome-variant subworlds exist, generate correctly, satisfy their target Zone flag, and load safely with their source mod disabled) -- ready for Phase 6/7 to complete the requirement by actually routing bosses into them via `BossArenaRoutingRegistry.Register<T>()`. Dungeon and Sulphurous Sea are out of scope per D-07 (09-CONTEXT.md).
</success_criteria>

<output>
After completion, create `.planning/phases/09-biome-dependent-subworld-coverage/09-07-SUMMARY.md`, recording the actual live test results for both checkpoint parts and confirmation of the debug file's removal.
</output>
