---
phase: 08-full-pipeline-verification-tracker-confirmation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: []
autonomous: false
requirements: [VERIFY-01, VERIFY-03]

must_haves:
  truths:
    - "Live in-game test confirms Redemption.Globals.RedeBossDowned.downedThorn == true after using the carrier item in the main world following a real Thorn kill in the subworld, Thorn's chat message + Alignment change replay correctly, and Boss Checklist's tracker UI shows Thorn as defeated"
    - "Live in-game test confirms CatalystMod.WorldDefeats.downedAstrageldon == true after using the carrier item in the main world following a real Astrageldon kill fought in the DEFAULT BossArenaSubworld (not an Astral-biome variant -- 08-RESEARCH.md Pitfall 3, since 06-02-SUMMARY.md deliberately did not register Astrageldon to BossArenaRoutingRegistry), MetanovaGenerator.Generate()'s ore-vein tiles visibly generate, and Boss Checklist's tracker UI shows Astrageldon as defeated"
    - "The Moon-Lord-lockout eligibility delegate (SummonItemRegistry.canSummon, added in 06-02) is confirmed live: AstralCommunicator's Test1 redirect is blocked (no subworld entry, no item consumed) when Moon Lord is downed and Astrageldon is not yet downed, and continues to work normally once Astrageldon is downed even after Moon Lord is later downed too"
    - "The mod continues to load and run safely with Redemption disabled and, separately, with CatalystMod disabled -- no JITException naming Integrations/RedemptionIntegration.cs or Integrations/CatalystIntegration.cs in Logs/client.log"
  artifacts: []
  key_links: []
---

<objective>
Live-verify Thorn (Redemption) and Astrageldon (CatalystMod), whose registration code (`Integrations/RedemptionIntegration.cs`, `Integrations/CatalystIntegration.cs`) is complete and building cleanly (06-02, merged) but has never been exercised live -- Phase 6's own `06-03-PLAN.md` live-verification checkpoint has not yet executed as of this planning session (no `06-03-SUMMARY.md`, `check.md`'s checkboxes unchecked). This plan performs that same live verification under Phase 8's umbrella, extended with explicit Boss Checklist tracker-UI confirmation (VERIFY-03) and the new-this-session Moon-Lord-lockout check.

Purpose: Closes VERIFY-01/VERIFY-03 for Thorn and Astrageldon. If `06-03-SUMMARY.md` does not yet exist when this plan executes, this checkpoint's results also satisfy Phase 6's own outstanding `06-03` plan -- avoid a duplicate live test, and note in this plan's SUMMARY that Phase 6 can be closed alongside Phase 8. If `06-03-SUMMARY.md` already exists by the time this plan executes (Phase 6 closed independently), cite that result and only perform the two Phase-8-specific additions (Boss Checklist tracker-UI check + Moon Lord lockout cases) rather than re-running the full 16-step checklist, per 08-RESEARCH.md Pitfall 4.
Output: Live-confirmed Thorn/Astrageldon pipeline + Boss Checklist recognition + Moon Lord lockout behavior + Redemption/CatalystMod-disabled load safety. Recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-full-pipeline-verification-tracker-confirmation/08-CONTEXT.md
@.planning/phases/08-full-pipeline-verification-tracker-confirmation/08-RESEARCH.md
@.planning/phases/06-redemption-catalystmod-integration/06-03-PLAN.md
@.planning/phases/06-redemption-catalystmod-integration/check.md
@.planning/phases/06-redemption-catalystmod-integration/06-02-SUMMARY.md
@docs/WORLD_BACKUP_GUIDANCE.md
@Integrations/RedemptionIntegration.cs
@Integrations/CatalystIntegration.cs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live Thorn/Astrageldon pipeline + Boss Checklist + Moon Lord lockout checkpoint</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/RedemptionIntegration.cs (RegisterThorn/ApplyThornDowned/IsThornDowned -- the exact side effects this checkpoint confirms replay correctly: chat message, RedeWorld.Alignment += 2)
    - Integrations/CatalystIntegration.cs (RegisterAstrageldon/ApplyAstrageldonDowned/IsAstrageldonDowned -- the exact WorldGen side effect this checkpoint confirms: MetanovaGenerator.Generate() ore-vein generation, and the SummonItemRegistry.canSummon Moon-Lord-lockout delegate)
    - .planning/phases/06-redemption-catalystmod-integration/06-03-PLAN.md Task 1 (the exact 16-step Part A-D checklist this task follows verbatim, extended here with Boss-Checklist-tracker-UI-specific confirmation)
    - .planning/phases/06-redemption-catalystmod-integration/check.md section 3 (the Moon Lord lockout block/unblock case -- new-this-session item not in 06-03-PLAN.md's original text)
    - .planning/phases/08-full-pipeline-verification-tracker-confirmation/08-RESEARCH.md "Pitfall 3" (Astrageldon has NO Astral-biome routing -- confirm testing in the default arena, not Astral, and do not treat this as a bug)
    - docs/WORLD_BACKUP_GUIDANCE.md
  </read_first>
  <action>
    1. Check whether `.planning/phases/06-redemption-catalystmod-integration/06-03-SUMMARY.md` already exists. If it does, Phase 6 already closed this checkpoint independently -- skip straight to step 5 below (citation mode) instead of re-running steps 2-4.

    2. If `06-03-SUMMARY.md` does not exist, follow `06-03-PLAN.md` Task 1's Parts A-D (16 steps) exactly as written: enable Redemption/CatalystMod, back up the world per docs/WORLD_BACKUP_GUIDANCE.md, then Thorn kill -> carry -> apply (Part C, steps 5-10) and Astrageldon kill -> carry -> apply (Part D, steps 11-16).

    3. During steps 9 and 15 (the downed-flag check), additionally open Boss Checklist's tracker UI specifically (not just the internal flag or CheatSheet variable inspector) and confirm it shows Thorn / Astrageldon as defeated -- this explicit tracker-UI check is the concrete VERIFY-03 closure this task exists for.

    4. During step 12 (entering the subworld for Astrageldon), confirm the kill happens in the DEFAULT `BossArenaSubworld` (plain arena), not an Astral-biome variant -- per 08-RESEARCH.md Pitfall 3, `06-02-SUMMARY.md` deliberately did not register Astrageldon to `BossArenaRoutingRegistry` (no Zone dependency found in its decompiled source). This is expected behavior, not a defect to fix.

    5. Regardless of whether steps 2-4 were run live or cited from an existing `06-03-SUMMARY.md`, run check.md section 3's two Moon-Lord-lockout cases fresh (these are new-this-session items, never previously live-tested):
       - Block case: on a character where Moon Lord is downed and Astrageldon is NOT yet downed, hold `AstralCommunicator` and right-click the Test1 portal tile -- confirm NO subworld entry occurs and the item is NOT consumed.
       - Normal case: on a character where Astrageldon is downed first (`WorldDefeats.downedAstrageldon == true`), then Moon Lord is downed afterward -- confirm `AstralCommunicator`'s redirect still works normally on reuse.

    6. Re-use the Thorn/Astrageldon carrier items (or re-trigger their apply path) and confirm no duplicate chat message, no duplicate ore-vein generation, and no duplicate Boss Checklist entry (APPLY-04 idempotency) -- unless already confirmed via a cited `06-03-SUMMARY.md`.
  </action>
  <what-built>
    Phase 6 (06-02) registered Thorn (`redemption:thorn`) and Astrageldon (`catalyst:astrageldon`) into the existing BossRegistry/SummonItemRegistry pipeline, including a Moon-Lord-lockout eligibility delegate for Astrageldon's summon item. This task verifies the full subworld-kill -> carrier-item-drop -> main-world-apply pipeline actually works at runtime for both bosses -- including real side effects, Boss Checklist tracker-UI recognition, and the lockout behavior -- not just that the code compiles.
  </what-built>
  <how-to-verify>
    Follow the 6 numbered steps in the action above, citing `06-03-SUMMARY.md` instead of re-testing wherever it already covers a step.
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader boss-kill/carrier-item/Boss-Checklist-UI behavior (per 08-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - Thorn killed in the subworld, BossCoreItem tagged redemption:thorn dropped and used in the main world, RedeBossDowned.downedThorn confirmed true, chat message observed, Boss Checklist tracker UI shows Thorn defeated
    - Astrageldon killed in the DEFAULT BossArenaSubworld, BossCoreItem tagged catalyst:astrageldon dropped and used in the main world, WorldDefeats.downedAstrageldon confirmed true, ore-vein tiles observed generated, Boss Checklist tracker UI shows Astrageldon defeated
    - Moon Lord lockout block case confirmed (no subworld entry, no item consumption) and normal-reuse case confirmed (redirect still works once Astrageldon is already downed)
    - Re-using either carrier item does not duplicate any side effect (APPLY-04)
    - If this task's results also close Phase 6's own 06-03 plan, that fact is explicitly noted in this plan's SUMMARY
  </acceptance_criteria>
  <done>Thorn's and Astrageldon's full subworld-kill-to-main-world-apply pipelines, Boss Checklist recognition, and Moon Lord lockout behavior are empirically confirmed working, satisfying VERIFY-01/VERIFY-03 for both bosses.</done>
  <resume-signal>Type "thorn and astrageldon verified" if all steps (pipeline, Boss Checklist UI, Moon Lord lockout, idempotency) passed, or describe exactly which step failed and what was observed instead.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Redemption-disabled and CatalystMod-disabled load-safety checkpoint</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/RedemptionIntegration.cs, Integrations/CatalystIntegration.cs (confirm every method touching a Redemption/CatalystMod type carries its own [JITWhenModsEnabled] tag -- the property this checkpoint is actually testing at runtime)
    - .planning/phases/06-redemption-catalystmod-integration/06-03-PLAN.md Task 2 (the exact 10-step Part A-B checklist this task follows verbatim -- a pure JIT-safety check with no Phase-8-specific addition needed)
  </read_first>
  <action>
    1. Check whether `.planning/phases/06-redemption-catalystmod-integration/06-03-SUMMARY.md` already exists and already recorded this exact result. If so, cite it in this plan's SUMMARY instead of re-testing.
    2. Otherwise, follow `06-03-PLAN.md` Task 2's Parts A-B (10 steps) exactly: disable Redemption only, confirm clean load + no exception in `Logs/client.log` naming `RedemptionIntegration`, re-enable; then disable CatalystMod only, confirm clean load + no exception naming `CatalystIntegration`, re-enable.
  </action>
  <what-built>
    Phase 6's two integration files isolate every Redemption/CatalystMod type reference behind a named, separately-`[JITWhenModsEnabled]`-tagged method, following the locked project-wide discipline established in Phase 4/9. This task verifies that isolation boundary actually prevents a JIT crash at runtime when each mod is genuinely absent.
  </what-built>
  <how-to-verify>
    Follow the 2 numbered steps in the action above (citing 06-03-SUMMARY.md if it already covers this result).
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader mod-load JIT behavior (per 08-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - tModLoader loaded successfully with Redemption disabled, no JIT crash or exception dialog, Logs/client.log contains no RedemptionIntegration exception
    - tModLoader loaded successfully with CatalystMod disabled, no JIT crash or exception dialog, Logs/client.log contains no CatalystIntegration exception
    - Both mods re-enabled afterward, mod list confirmed restored to normal
  </acceptance_criteria>
  <done>Both integrations are empirically confirmed to load and run safely at runtime with their respective source mod disabled.</done>
  <resume-signal>Type "mod-disabled safety verified" if both parts loaded cleanly with no relevant exceptions in the log, or paste the exact exception/error otherwise.</resume-signal>
</task>

</tasks>

<verification>
Both checkpoint tasks in this plan have no automated equivalent (per 08-VALIDATION.md -- no in-game test harness exists in this project). Verification is entirely the human-confirmed resume-signals: "thorn and astrageldon verified" (Task 1) and "mod-disabled safety verified" (Task 2).
</verification>

<success_criteria>
- Live test confirms Thorn's and Astrageldon's downed flags + side effects apply correctly, with Boss Checklist tracker-UI recognition (VERIFY-01/03)
- Moon Lord lockout eligibility delegate confirmed live for both the block and normal-reuse cases
- Live test confirms Redemption-disabled and CatalystMod-disabled load safety, no JIT crash
- Phase 6's own outstanding 06-03 checkpoint is closed alongside this plan if not already done independently
</success_criteria>

<output>
After completion, create `.planning/phases/08-full-pipeline-verification-tracker-confirmation/08-02-SUMMARY.md`, recording the actual live test results (or citations) for both tasks, and explicitly noting whether this also closes Phase 6's `06-03` plan.
</output>
