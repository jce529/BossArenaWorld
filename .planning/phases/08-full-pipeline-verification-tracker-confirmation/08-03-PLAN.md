---
phase: 08-full-pipeline-verification-tracker-confirmation
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: []
autonomous: false
requirements: [VERIFY-01, VERIFY-03]

must_haves:
  truths:
    - "Live in-game test confirms ContinentOfJourney.DownedBossSystem.downedGoblinChariot == true after using the carrier item in the main world following a real Goblin Chariot kill in the subworld"
    - "Boss Checklist's tracker UI shows Goblin Chariot as downed after the carrier item is used, confirming Homeward Journey's own bundled CoJ_BossChecklist.cs integration recognizes the flag with no extra work from this project (closes 07-RESEARCH.md Open Question 2 and doubles as this plan's VERIFY-03 contribution)"
    - "Re-using the Goblin Chariot carrier item does not duplicate any side effect or Boss Checklist entry (APPLY-04 idempotency)"
    - "The mod continues to load and run safely with ContinentOfJourney (Homeward Journey) disabled -- no JITException naming Integrations/HomewardJourneyIntegration.cs in Logs/client.log"
  artifacts: []
  key_links: []
---

<objective>
Live-verify Goblin Chariot (ContinentOfJourney / Homeward Journey), whose registration file `Integrations/HomewardJourneyIntegration.cs` and its `build.txt`/`.csproj` weak-reference wiring are present in the working tree and building cleanly (`dotnet build BossArenaSubWorld.csproj` confirmed exit 0 during this session's re-verification), but has never been exercised live -- Phase 7's own `07-02-PLAN.md` live-verification checkpoint has not yet executed as of this planning session (no `07-01-SUMMARY.md`/`07-02-SUMMARY.md`). This plan performs that same live verification under Phase 8's umbrella.

Purpose: Closes VERIFY-01/VERIFY-03 for Goblin Chariot, the fifth and final v1 mod integration. If Phase 7's own `07-01-SUMMARY.md`/`07-02-SUMMARY.md` do not yet exist when this plan executes, this checkpoint's results also satisfy Phase 7's outstanding plans -- avoid a duplicate live test, and note in this plan's SUMMARY that Phase 7 can be closed alongside Phase 8. If those SUMMARYs already exist by the time this plan executes (Phase 7 closed independently), cite that result instead of re-testing, per 08-RESEARCH.md Pitfall 4.
Output: Live-confirmed Goblin Chariot pipeline + Boss Checklist / CoJ_BossChecklist.cs recognition + ContinentOfJourney-disabled load safety. Recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-full-pipeline-verification-tracker-confirmation/08-CONTEXT.md
@.planning/phases/08-full-pipeline-verification-tracker-confirmation/08-RESEARCH.md
@.planning/phases/07-noxusboss-continentofjourney-daybreak-integration/07-01-PLAN.md
@.planning/phases/07-noxusboss-continentofjourney-daybreak-integration/07-02-PLAN.md
@docs/WORLD_BACKUP_GUIDANCE.md
@Integrations/HomewardJourneyIntegration.cs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live Goblin Chariot pipeline + Boss Checklist recognition checkpoint</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/HomewardJourneyIntegration.cs (RegisterGoblinChariot/ApplyGoblinChariotDowned/IsGoblinChariotDowned -- the exact flag write this checkpoint confirms replays correctly)
    - .planning/phases/07-noxusboss-continentofjourney-daybreak-integration/07-01-PLAN.md (confirm build.txt/BossArenaSubWorld.csproj already declare the ContinentOfJourney weak reference before this checkpoint runs -- re-run `dotnet build BossArenaSubWorld.csproj` first if unsure)
    - .planning/phases/07-noxusboss-continentofjourney-daybreak-integration/07-02-PLAN.md Task 1 (the exact 10-step Part A-C checklist this task follows verbatim -- already includes the Boss Checklist / CoJ_BossChecklist.cs confirmation step)
    - docs/WORLD_BACKUP_GUIDANCE.md
  </read_first>
  <action>
    1. Check whether `.planning/phases/07-noxusboss-continentofjourney-daybreak-integration/07-01-SUMMARY.md` and `07-02-SUMMARY.md` already exist. If both do, Phase 7 already closed this checkpoint independently -- cite that result in this plan's SUMMARY instead of re-testing.

    2. If not, first confirm Task 1's registration code actually compiles: run `dotnet build BossArenaSubWorld.csproj` from the project root and confirm exit code 0 with `Integrations/HomewardJourneyIntegration.cs` present (this file already exists in the working tree as of this planning session; if it is missing, Phase 7's Plan 01 has not been executed yet and this task cannot proceed -- report that blocker instead).

    3. Follow `07-02-PLAN.md` Task 1's Parts A-C (10 steps) exactly as written: subscribe/enable ContinentOfJourney (Homeward Journey, Steam Workshop id 2930931197) alongside the existing mods, back up the world per docs/WORLD_BACKUP_GUIDANCE.md, acquire `PurpleFlareGun`, redirect into the subworld via the Test1 portal, confirm Goblin Chariot auto-summons, kill it, confirm the `BossCoreItem` tagged `continentofjourney:goblin_chariot` drops, return to the main world, use the item, and confirm:
       - `ContinentOfJourney.DownedBossSystem.downedGoblinChariot` becomes `true`
       - Boss Checklist's tracker UI shows Goblin Chariot as downed, confirming Homeward Journey's own bundled `CoJ_BossChecklist.cs` integration recognizes the flag correctly (closes 07-RESEARCH.md Open Question 2)
       - No JITException or runtime exception occurs during `NPC.SetEventFlagCleared`

    4. Re-use the carrier item and confirm no double-apply / no duplicate Boss Checklist entry (APPLY-04 idempotency).
  </action>
  <what-built>
    Phase 7's `Integrations/HomewardJourneyIntegration.cs` registers Goblin Chariot (`continentofjourney:goblin_chariot`) into the existing BossRegistry/SummonItemRegistry pipeline using ContinentOfJourney's actual public-static-field downed-tracking API. This task verifies the full subworld-kill -> carrier-item-drop -> main-world-apply pipeline actually works at runtime, including Boss Checklist's own external recognition of the flag via Homeward Journey's bundled integration, not just that the code compiles.
  </what-built>
  <how-to-verify>
    Follow the 4 numbered steps in the action above, citing existing Phase 7 SUMMARYs instead of re-testing wherever they already cover a step.
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader boss-kill/carrier-item/Boss-Checklist-UI behavior (per 08-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build BossArenaSubWorld.csproj exits 0 with Integrations/HomewardJourneyIntegration.cs present, confirmed before any live test
    - ContinentOfJourney (Homeward Journey) subscribed, enabled, and loaded without error
    - World backed up per docs/WORLD_BACKUP_GUIDANCE.md before any live test
    - Goblin Chariot killed in the subworld, BossCoreItem tagged continentofjourney:goblin_chariot dropped and used in the main world, DownedBossSystem.downedGoblinChariot confirmed true
    - Boss Checklist's tracker UI confirms Goblin Chariot recognized as downed after the carrier item is used
    - Re-using the carrier item does not duplicate any side effect or Boss Checklist entry (APPLY-04)
    - If this task's results also close Phase 7's own 07-01/07-02 plans, that fact is explicitly noted in this plan's SUMMARY
  </acceptance_criteria>
  <done>Goblin Chariot's full subworld-kill-to-main-world-apply pipeline, including Boss Checklist recognition via Homeward Journey's own CoJ_BossChecklist.cs integration, is empirically confirmed working, satisfying VERIFY-01/VERIFY-03 for the final v1 mod integration.</done>
  <resume-signal>Type "goblin chariot verified" if all steps passed, or describe exactly which step failed and what was observed instead.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: ContinentOfJourney-disabled load-safety checkpoint</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/HomewardJourneyIntegration.cs (confirm every method touching a ContinentOfJourney type carries its own [JITWhenModsEnabled("ContinentOfJourney")] tag -- the property this checkpoint is actually testing at runtime)
    - .planning/phases/07-noxusboss-continentofjourney-daybreak-integration/07-02-PLAN.md Task 2 (the exact 5-step checklist this task follows verbatim -- a pure JIT-safety check with no Phase-8-specific addition needed)
  </read_first>
  <action>
    1. Check whether `.planning/phases/07-noxusboss-continentofjourney-daybreak-integration/07-02-SUMMARY.md` already exists and already recorded this exact result. If so, cite it in this plan's SUMMARY instead of re-testing.
    2. Otherwise, follow `07-02-PLAN.md` Task 2's 5 steps exactly: disable ContinentOfJourney only (leave CalamityMod, SpiritMod, Redemption, CatalystMod, SubworldLibrary, BossArenaSubWorld enabled), relaunch, confirm clean load with no JIT crash/exception dialog, check `Logs/client.log` for no exception naming `HomewardJourneyIntegration`, confirm other bosses' features still work, then re-enable ContinentOfJourney.
  </action>
  <what-built>
    Phase 7's integration file isolates every ContinentOfJourney type reference behind a named, separately-`[JITWhenModsEnabled("ContinentOfJourney")]`-tagged method, following the locked project-wide discipline established in Phase 4/9. This task verifies that isolation boundary actually prevents a JIT crash at runtime when the mod is genuinely absent.
  </what-built>
  <how-to-verify>
    Follow the 2 numbered steps in the action above (citing 07-02-SUMMARY.md if it already covers this result).
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader mod-load JIT behavior (per 08-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - tModLoader loaded successfully with ContinentOfJourney disabled, no JIT crash or exception dialog observed
    - Logs/client.log contains no ContinentOfJourney-related exception naming HomewardJourneyIntegration
    - Other registered bosses' features (King Slime, Hive Mind, Infernon, Thorn, Astrageldon) still function normally
    - ContinentOfJourney re-enabled afterward, mod list confirmed restored to normal
  </acceptance_criteria>
  <done>The integration is empirically confirmed to load and run safely at runtime with ContinentOfJourney disabled, completing MOD-06/v1 mod coverage's live-safety confirmation.</done>
  <resume-signal>Type "mod-disabled safety verified" if the mod loaded cleanly with no relevant exceptions in the log, or paste the exact exception/error otherwise.</resume-signal>
</task>

</tasks>

<verification>
Both checkpoint tasks in this plan have no automated equivalent (per 08-VALIDATION.md -- no in-game test harness exists in this project). Verification is entirely the human-confirmed resume-signals: "goblin chariot verified" (Task 1) and "mod-disabled safety verified" (Task 2).
</verification>

<success_criteria>
- Live test confirms Goblin Chariot's downed flag applies correctly and is recognized by Boss Checklist via Homeward Journey's own CoJ_BossChecklist.cs integration (VERIFY-01/03)
- Live test confirms the mod loads and runs safely with ContinentOfJourney disabled, no JIT crash
- Phase 7's own outstanding 07-01/07-02 plans are closed alongside this plan if not already done independently, completing v1 mod coverage (MOD-06)
</success_criteria>

<output>
After completion, create `.planning/phases/08-full-pipeline-verification-tracker-confirmation/08-03-SUMMARY.md`, recording the actual live test results (or citations) for both tasks, and explicitly noting whether this also closes Phase 7's `07-01`/`07-02` plans.
</output>
