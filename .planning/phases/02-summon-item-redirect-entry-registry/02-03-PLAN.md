---
phase: 02-summon-item-redirect-entry-registry
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - Debug/SubworldDebugCommands.cs
autonomous: false
requirements: [SUBW-01, SUBW-02, SUBW-03, SUBW-04]

must_haves:
  truths:
    - "Placing Test1Tile and right-clicking it while holding Slime Crown redirects the player into the boss-arena subworld -- King Slime is never spawned in the main world at any point during the interaction"
    - "King Slime automatically spawns near the player immediately upon arrival in the subworld, with no per-boss spawn code invoked"
    - "Slime Crown remains in the player's inventory after the full redirect round trip -- it is never consumed"
    - "Right-clicking Test1Tile while holding an unregistered item does nothing -- no redirect, no chat message, no subworld entry"
    - "All Phase 1/2 debug tooling (Debug/SubworldDebugCommands.cs) is removed once the real redirect is confirmed working end-to-end, per Phase 1's D-02 decision"
  artifacts: []
  key_links: []
---

<objective>
Empirically prove, via a real in-game test, that the portal-tile redirect built in Plans 02-01/02-02 satisfies all four of this phase's requirements (SUBW-01 through SUBW-04) end-to-end -- then remove the temporary debug tooling (`Debug/SubworldDebugCommands.cs`, including this phase's own `/bossarena-givetestitems` addition) now that the real player-facing mechanism replaces it, per Phase 1's D-02 decision and 02-RESEARCH.md's Open Question 1 recommendation (keep debug tooling through live verification, delete only after it passes).

Purpose: Validate SUBW-01/02/03/04 live (this project has no automated test framework for in-game behavior, per 02-VALIDATION.md), and close out this phase's temporary tooling debt.
Output: A completed manual test run with confirmed results, and `Debug/SubworldDebugCommands.cs` deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-summon-item-redirect-entry-registry/02-CONTEXT.md
@.planning/phases/02-summon-item-redirect-entry-registry/02-RESEARCH.md
@.planning/phases/02-summon-item-redirect-entry-registry/02-VALIDATION.md
@Tiles/Test1Tile.cs
@Items/Test1Item.cs
@Debug/SubworldDebugCommands.cs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the live portal-tile redirect test</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Tiles/Test1Tile.cs, Items/Test1Item.cs (Plan 02-02 -- what's being exercised)
    - Systems/SummonItemRegistry.cs, Systems/BossSummonPlayer.cs (Plan 02-01 -- the backend being exercised)
    - Debug/SubworldDebugCommands.cs (the temporary /bossarena-givetestitems command used to obtain test items, and the pre-existing /bossarena-checkflag command usable as a supplementary check)
    - .planning/phases/02-summon-item-redirect-entry-registry/02-CONTEXT.md (D-08 King Slime/Slime Crown proof pair, D-11 chat message)
  </read_first>
  <action>Run the full portal-tile redirect test in-game, following the exact steps in how-to-verify below, and report the result.</action>
  <what-built>
    Plans 02-01/02-02 built: a data-driven summon-item registry mapping Slime Crown -> King Slime (Systems/SummonItemRegistry.cs); a generic arrival auto-summon hook (Systems/BossSummonPlayer.cs); a placeable portal tile with a right-click redirect trigger (Tiles/Test1Tile.cs) and its placing item (Items/Test1Item.cs); and a temporary debug command to obtain both test items (/bossarena-givetestitems). This task verifies the full chain works together live and proves SUBW-01 through SUBW-04.
  </what-built>
  <how-to-verify>
    On a disposable or otherwise safe test world (per docs/WORLD_BACKUP_GUIDANCE.md), in singleplayer:

    1. Type `/bossarena-givetestitems` in chat. Confirm you receive 1x Test1 portal item and 1x Slime Crown.
    2. Place the Test1 item as a tile somewhere convenient. Confirm it renders as a solid, indigo/purple single tile (placeholder texture) -- not a "missing texture" magenta/checker placeholder.
    3. Hold an item that is NOT Slime Crown (anything else) and right-click the placed Test1 tile. Confirm NOTHING happens -- no chat message, no screen transition, you remain in the main world (SUBW-02 negative-gate check).
    4. Switch to holding the Slime Crown. Right-click the placed Test1 tile. Confirm:
       - A chat message appears (D-11).
       - King Slime does NOT appear anywhere in the main world at this moment (SUBW-02 "never spawns in main world" check).
       - The screen transitions into the boss-arena subworld (SUBW-03).
    5. Immediately upon arrival in the subworld, confirm King Slime has automatically spawned near you (aggroed/visible) -- with no manual summon action taken (SUBW-04 auto-summon check).
    6. Check your inventory. Confirm the Slime Crown is still present -- it was NOT consumed by the redirect (SUBW-04 "item not consumed" check).
    7. Optional: fight King Slime, or ignore it. Either is fine -- this plan does not test the kill/carrier-item pipeline (that's Phase 3).
    8. Press ESC to open the pause menu. Confirm a "Return" button (added automatically by SubworldLibrary, since BossArenaSubworld does not set noReturn) is present -- click it to exit back to the main world.
    9. Confirm you're back in the main world, standing near the placed Test1 tile, with your pre-test inventory otherwise intact.
  </how-to-verify>
  <verify>Step 4's redirect enters the subworld without King Slime ever appearing in the main world; step 5 confirms auto-spawn; step 6 confirms Slime Crown still present</verify>
  <acceptance_criteria>
    - Step 1 grants both test items
    - Step 2 tile renders correctly (no missing-texture placeholder)
    - Step 3 confirms no interaction fires for an unregistered held item
    - Step 4 confirms redirect fires only for Slime Crown, with no King Slime spawn in the main world
    - Step 5 confirms King Slime auto-spawns in the subworld with no manual action
    - Step 6 confirms Slime Crown was not consumed
    - Step 8-9 confirm a clean return to the main world via SubworldLibrary's own Return button
  </acceptance_criteria>
  <done>All four requirements (SUBW-01 through SUBW-04) are empirically confirmed working end-to-end via the real portal-tile interaction, with no debug commands used for the redirect itself (only for initial item acquisition).</done>
  <resume-signal>Report the result of each numbered step above. Type "redirect verified" if all steps passed as expected, or describe exactly what went wrong (which step, what was observed instead) otherwise.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove Phase 1/2 debug tooling</name>
  <files>Debug/SubworldDebugCommands.cs</files>
  <read_first>
    - Debug/SubworldDebugCommands.cs (the file being deleted)
    - .planning/phases/01-subworld-skeleton-isolation-proof/01-CONTEXT.md (D-02 -- debug commands deleted once the real redirect fully lands and is verified)
  </read_first>
  <action>
    Only perform this task after Task 1's checkpoint returns "redirect verified" (or equivalent explicit confirmation). Delete the entire file Debug/SubworldDebugCommands.cs (all four classes: BossArenaEnterCommand, BossArenaExitCommand, BossArenaCheckFlagCommand, BossArenaGiveTestItemsCommand). This tooling's job -- making the subworld reachable/testable before the real redirect existed -- is now fully superseded by Tiles/Test1Tile.cs's NewRightClick interaction (Plan 02-02) and SubworldLibrary's own built-in Return button (confirmed in Task 1's live test). Grep the rest of the codebase for any reference to the four deleted class names or the `BossArenaSubWorld.Debug` namespace to confirm nothing else depends on this file before deleting (none are expected -- ModCommand/ModType classes are auto-discovered by tModLoader, never manually referenced elsewhere).
  </action>
  <verify>
    <automated>cd "C:\Users\chang\Documents\My Games\Terraria\tModLoader\ModSources\BossArenaSubWorld" && dotnet build BossArenaSubWorld.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Debug/SubworldDebugCommands.cs no longer exists on disk
    - The Debug/ directory is empty or removed
    - No remaining file in the repo references BossArenaEnterCommand, BossArenaExitCommand, BossArenaCheckFlagCommand, or BossArenaGiveTestItemsCommand (grep across *.cs returns no matches)
    - dotnet build BossArenaSubWorld.csproj exits with code 0
  </acceptance_criteria>
  <done>Debug/SubworldDebugCommands.cs is deleted, the project still builds cleanly, and the boss-arena subworld is now reachable only via the real Test1Tile redirect.</done>
</task>

</tasks>

<verification>
This plan's real verification is Task 1's live checkpoint -- no automated test framework exists for in-game tModLoader behavior (per 02-VALIDATION.md). Task 2's `dotnet build` confirms the codebase remains in a compiling state after debug-tooling removal.
</verification>

<success_criteria>
- Live test confirms: Test1Tile places and renders correctly; right-click with an unregistered item does nothing; right-click with Slime Crown redirects into the subworld without ever spawning King Slime in the main world; King Slime auto-spawns on arrival; Slime Crown is not consumed; the player can return to the main world via SubworldLibrary's built-in Return button
- Debug/SubworldDebugCommands.cs is deleted and the project still builds with 0 errors, 0 warnings
</success_criteria>

<output>
After completion, create `.planning/phases/02-summon-item-redirect-entry-registry/02-03-SUMMARY.md`, recording the actual live test results for each of the four requirements (SUBW-01 through SUBW-04).
</output>
