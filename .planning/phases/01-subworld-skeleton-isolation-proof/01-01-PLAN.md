---
phase: 01-subworld-skeleton-isolation-proof
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - global.json
  - build.txt
  - docs/WORLD_BACKUP_GUIDANCE.md
autonomous: true
requirements: [VERIFY-02]

must_haves:
  truths:
    - "The project builds successfully with .NET SDK 8.0.x, not the machine-default 10.0.201"
    - "SubworldLibrary is declared as a strong dependency the build pipeline can resolve"
    - "World-backup guidance exists documenting save paths and backup steps for later live-save testing phases"
  artifacts:
    - path: "global.json"
      provides: "Pins dotnet SDK resolution to an installed 8.0.x version"
      contains: "8.0.424"
    - path: "build.txt"
      provides: "Declares modReferences = SubworldLibrary"
      contains: "modReferences = SubworldLibrary"
    - path: "docs/WORLD_BACKUP_GUIDANCE.md"
      provides: "VERIFY-02 world-backup guidance deliverable"
      contains: "Worlds"
  key_links: []
---

<objective>
Establish the build-environment prerequisites this entire phase (and every later phase) depends on: pin the .NET SDK to 8.0.x so `dotnet build`/`dotnet msbuild` never silently compiles against SDK 10.0.201, declare `SubworldLibrary` as a strong `modReferences` dependency in `build.txt` so subsequent plans can reference its types, and write the VERIFY-02 world-backup guidance document.

Purpose: Without this Wave 0 setup, every later task in the phase either fails to build (wrong SDK) or fails to compile (missing SubworldLibrary reference). This is the single blocking prerequisite for all of Phase 1.
Output: `global.json` (SDK pin), updated `build.txt` (SubworldLibrary reference), `docs/WORLD_BACKUP_GUIDANCE.md` (VERIFY-02 deliverable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-subworld-skeleton-isolation-proof/01-CONTEXT.md
@.planning/phases/01-subworld-skeleton-isolation-proof/01-RESEARCH.md
@.planning/phases/01-subworld-skeleton-isolation-proof/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin .NET SDK via global.json</name>
  <files>global.json</files>
  <read_first>
    - BossArenaSubWorld.csproj (confirms TargetFramework net8.0 is hardcoded via tModLoader.targets import, unaffected by SDK pin but confirms context)
    - .planning/phases/01-subworld-skeleton-isolation-proof/01-RESEARCH.md, section "Common Pitfalls -> Pitfall 5: SDK version mismatch" (exact rationale and recommended global.json shape)
  </read_first>
  <action>
    Create `global.json` at the project root (same directory as `BossArenaSubWorld.csproj`) with exactly this content:

    {
      "sdk": {
        "version": "8.0.424",
        "rollForward": "latestFeature"
      }
    }

    This machine has SDKs 8.0.424, 9.0.310, and 10.0.201 installed (confirmed via `dotnet --list-sdks`). Without this pin, `dotnet --version` in this directory resolves to 10.0.201, which CLAUDE.md explicitly prohibits ("avoid .NET 9.0 and .NET 10.0, as those will not work"). `rollForward: latestFeature` allows picking up 8.0.x patch updates without breaking on an exact patch match.

    After creating the file, run `dotnet --version` from the project root and confirm it now prints `8.0.424` (not `10.0.201`).
  </action>
  <verify>
    <automated>cd "C:\Users\chang\Documents\My Games\Terraria\tModLoader\ModSources\BossArenaSubWorld" && dotnet --version</automated>
  </verify>
  <acceptance_criteria>
    - `global.json` exists at the project root
    - `global.json` contains the exact string "version": "8.0.424"
    - Running `dotnet --version` from the project root prints `8.0.424`, not `10.0.201` or `9.0.310`
  </acceptance_criteria>
  <done>`dotnet --version` in the project directory resolves to 8.0.424.</done>
</task>

<task type="auto">
  <name>Task 2: Add SubworldLibrary modReference and verify build</name>
  <files>build.txt</files>
  <read_first>
    - build.txt (current state -- only has displayName/author/version, no modReferences line yet)
    - BossArenaSubWorld.csproj (confirms tModLoader.targets import chain that resolves modReferences)
    - .planning/phases/01-subworld-skeleton-isolation-proof/01-RESEARCH.md, section "Standard Stack -> Installation (build.txt additions)"
  </read_first>
  <action>
    Edit `build.txt` to add a `modReferences` line. Current content is:
    displayName = 보스 진행도 동기화되는 서브월드
    author = HiPoDEV
    version = 0.1

    Add this line (new line, anywhere after the existing three lines):
    modReferences = SubworldLibrary

    This is a strong reference (not `weakReferences`), per CLAUDE.md and 01-RESEARCH.md -- the entire subworld mechanic depends on SubworldLibrary being present, so the mod should not load without it.

    After editing, run (in order, from the project root):
    dotnet restore
    dotnet build BossArenaSubWorld.csproj

    If `dotnet restore` or `dotnet build` reports "project.assets.json not found", re-run `dotnet restore` first, then `dotnet build` again. The build must succeed (exit code 0) even though no code yet references any SubworldLibrary type -- this task only proves the reference resolves, not that it's used.
  </action>
  <verify>
    <automated>cd "C:\Users\chang\Documents\My Games\Terraria\tModLoader\ModSources\BossArenaSubWorld" && dotnet restore && dotnet build BossArenaSubWorld.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `build.txt` contains the exact line: modReferences = SubworldLibrary
    - `dotnet build BossArenaSubWorld.csproj` exits with code 0 (build succeeded)
    - Build output contains no errors referencing SubworldLibrary resolution failure
  </acceptance_criteria>
  <done>build.txt declares modReferences = SubworldLibrary, and `dotnet build BossArenaSubWorld.csproj` succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Write VERIFY-02 world-backup guidance document</name>
  <files>docs/WORLD_BACKUP_GUIDANCE.md</files>
  <read_first>
    - .planning/phases/01-subworld-skeleton-isolation-proof/01-RESEARCH.md, section "Code Examples -> World-backup guidance (VERIFY-02 deliverable)" (exact save paths and recommended content)
    - .planning/phases/01-subworld-skeleton-isolation-proof/01-CONTEXT.md (D-13, D-14 -- this phase's own testing uses a disposable world and does NOT exercise this guidance; it is forward-looking documentation for Phases 4-8)
  </read_first>
  <action>
    Create the `docs/` directory and a new file `docs/WORLD_BACKUP_GUIDANCE.md`. Write a Markdown document with these exact sections and facts (executor may format prose naturally, but must include every fact below verbatim):

    Title: "World Backup Guidance"

    Applies-to note: this guidance applies to live testing against a real save (starting Phase 4, when content mods like Calamity/Spirit are enabled). Phase 1 does NOT require this procedure -- Phase 1 testing happens on a disposable, mod-free test world (see D-13 in 01-CONTEXT.md).

    Section "Default tModLoader Save Locations (Windows)" must state:
    - Worlds path: %UserProfile%\Documents\My Games\Terraria\tModLoader\Worlds\
    - Players path: %UserProfile%\Documents\My Games\Terraria\tModLoader\Players\
    - On this machine, resolved: C:\Users\chang\Documents\My Games\Terraria\tModLoader\Worlds\ and C:\Users\chang\Documents\My Games\Terraria\tModLoader\Players\

    Section "Backup Procedure (required before testing against a real save)" must list these three numbered steps:
    1. Before testing any phase against the real save (HiPo's_Terrarium), copy both the .wld file (and its .bak if present) from Worlds\ and the corresponding .plr file from Players\ to a separate, timestamped backup folder (e.g. Worlds\_backups\2026-08-13_pre-phase4\).
    2. Confirm the backup copy is readable (file size > 0, not zero-byte) before proceeding with any live test.
    3. Only after the backup is confirmed should live testing begin.

    Section "Subworld File Location" must state: subworld files (when a Subworld has ShouldSave = true) are stored under Worlds\<main-world-UniqueId-GUID>\<ModName>_<SubworldClassName>.wld. The Phase 1 boss-arena subworld (BossArenaSubworld) has ShouldSave = false, so no subworld file is ever written for it -- nothing to back up for the arena itself. Documented for later phases in case any subworld's ShouldSave value changes.

    Section "Phase 1 Testing Note" must state: Phase 1's own isolation-proof test (King Slime kill/exit test) runs on a fresh, disposable test world with only SubworldLibrary and this mod enabled -- no other content mods, and not the real save. No backup is required for that disposable world. This guidance document exists now so it is ready to follow starting Phase 4.
  </action>
  <verify>
    <automated>test -f "C:\Users\chang\Documents\My Games\Terraria\tModLoader\ModSources\BossArenaSubWorld\docs\WORLD_BACKUP_GUIDANCE.md" && echo FOUND</automated>
  </verify>
  <acceptance_criteria>
    - `docs/WORLD_BACKUP_GUIDANCE.md` exists
    - File contains the string "Worlds\" and the string "Players\"
    - File contains the string "HiPo's_Terrarium"
    - File contains the string "ShouldSave = false"
    - File contains the string "D-13"
  </acceptance_criteria>
  <done>docs/WORLD_BACKUP_GUIDANCE.md exists with all required sections and facts present.</done>
</task>

</tasks>

<verification>
Run `dotnet --version` (expect 8.0.424) and `dotnet build BossArenaSubWorld.csproj` (expect exit code 0) from the project root. Confirm `docs/WORLD_BACKUP_GUIDANCE.md` exists and contains the required facts listed in Task 3's acceptance criteria.
</verification>

<success_criteria>
- `global.json` pins the SDK to 8.0.424 and `dotnet --version` confirms it
- `build.txt` declares `modReferences = SubworldLibrary` and the project builds successfully
- `docs/WORLD_BACKUP_GUIDANCE.md` exists and documents world/player save paths, the backup procedure, and the Phase 1 testing exception
</success_criteria>

<output>
After completion, create `.planning/phases/01-subworld-skeleton-isolation-proof/01-01-SUMMARY.md`
</output>
