---
phase: 12-entry-and-exit-convenience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Systems/BossSummonPlayer.cs
  - Systems/BiomeOverridePlayer.cs
  - Tiles/Test1Tile.cs
  - Tiles/ReturnPortalTile.cs
  - Subworlds/ArenaBuilder.cs
  - Subworlds/ArenaPolishPass.cs
autonomous: false
requirements: [ENTRY-01, ENTRY-02, ENTRY-03]

must_haves:
  truths:
    - "Entering any arena subworld no longer auto-summons the boss immediately -- the player controls prep timing and uses their held boss-summon item directly whenever ready (ENTRY-01)"
    - "InfernumMode toggle is still forced active on subworld entry for bosses requiring it (RequiresInfernumToggle = true)"
    - "A visible return-point portal tile exists near spawn in the arena; using it safely exits the subworld via SubworldSystem.Exit() without bypassing the downed-flag restore guard (ENTRY-02)"
    - "SubworldLibrary's built-in Return button remains fully operational and unmodified (ENTRY-03)"
  artifacts:
    - path: "Systems/BossSummonPlayer.cs"
      provides: "Pre-summon environment setup on arrival (Infernum toggle activation) without immediate NPC.SpawnOnPlayer"
      contains: "class BossSummonPlayer : ModPlayer"
    - path: "Tiles/ReturnPortalTile.cs"
      provides: "ModTile placed in arena subworlds calling SubworldSystem.Exit() on right click"
      contains: "class ReturnPortalTile : ModTile"
    - path: "Subworlds/ArenaBuilder.cs"
      provides: "PlaceReturnPortal placement helper"
      contains: "PlaceReturnPortal"
    - path: "Subworlds/ArenaPolishPass.cs"
      provides: "Automatically places ReturnPortalTile near player spawn on the platform"
      contains: "ReturnPortalTile"
  key_links:
    - from: "Tiles/ReturnPortalTile.cs"
      to: "SubworldLibrary.SubworldSystem"
      via: "SubworldSystem.Exit()"
      pattern: "SubworldSystem\\.Exit"
    - from: "Subworlds/ArenaPolishPass.cs"
      to: "Tiles/ReturnPortalTile.cs"
      via: "ModContent.TileType<Tiles.ReturnPortalTile>()"
      pattern: "ReturnPortalTile"
---

<objective>
Implement Entry & Exit Convenience (Phase 12) divided into two sequential waves:
- Wave 1: Discontinue immediate auto-summon on entry to give players prep timing control (ENTRY-01), while preserving InfernumMode toggle priming and updating BiomeOverridePlayer.
- Wave 2: Implement ReturnPortalTile and wire its world-gen placement near spawn in ArenaPolishPass, calling SubworldSystem.Exit() (ENTRY-02, ENTRY-03).

Output: `Systems/BossSummonPlayer.cs`, `Systems/BiomeOverridePlayer.cs`, `Tiles/Test1Tile.cs`, `Tiles/ReturnPortalTile.cs`, `Subworlds/ArenaBuilder.cs`, and `Subworlds/ArenaPolishPass.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-entry-and-exit-convenience/12-RESEARCH.md
@.planning/phases/12-entry-and-exit-convenience/12-CONTEXT.md
@Systems/BossSummonPlayer.cs
@Systems/BiomeOverridePlayer.cs
@Systems/ForcedTimeSystem.cs
@Tiles/Test1Tile.cs
@Subworlds/ArenaBuilder.cs
@Subworlds/ArenaPolishPass.cs
</context>

<waves>

<wave number="1">
  <title>Wave 1: Entry Convenience & Infernum Gating (ENTRY-01)</title>
  <tasks>
    <task type="auto">
      <name>Task 1.1: Refactor BossSummonPlayer and Test1Tile for player-controlled summon timing</name>
      <files>Systems/BossSummonPlayer.cs, Tiles/Test1Tile.cs</files>
      <instruction>
        1. In `Systems/BossSummonPlayer.cs`:
           - Remove `NPC.SpawnOnPlayer(Player.whoAmI, PendingBossNpcType.Value);`.
           - Check `ForcedTimeSystem.ActiveArenaBossNpcType` on `OnEnterWorld()`: if `ModLoader.HasMod("InfernumMode") && BossRegistry.TryGetDefinitionForNpc(ForcedTimeSystem.ActiveArenaBossNpcType, out BossDefinition def) && def.RequiresInfernumToggle`, call `CalamityIntegration.ForceInfernumModeActiveInArena()`.
           - Deprecate/remove `PendingBossNpcType` assignment.
        2. In `Tiles/Test1Tile.cs`:
           - Update chat text to `"보스 아레나로 입장합니다. 준비가 되면 소환 아이템을 사용하세요."`.
           - Remove `BossSummonPlayer.PendingBossNpcType = bossNpcType;` line (retaining `ForcedTimeSystem.ActiveArenaBossNpcType = bossNpcType;` and `BossArenaRoutingRegistry.Enter(bossNpcType);`).
      </instruction>
    </task>
    <task type="auto">
      <name>Task 1.2: Update BiomeOverridePlayer to check IsAnyArenaActive</name>
      <files>Systems/BiomeOverridePlayer.cs</files>
      <instruction>
        In `Systems/BiomeOverridePlayer.cs`:
        - Change `if (!SubworldSystem.IsActive<BossArenaSubworld>()) return;` to `if (!BossArenaRoutingRegistry.IsAnyArenaActive()) return;` so the override infrastructure correctly covers all 9 arena subworlds.
      </instruction>
    </task>
  </tasks>
  <checkpoint>
    - Verify with `dotnet build`.
    - Report Wave 1 deliverables to user for inspection & approval before proceeding to Wave 2.
  </checkpoint>
</wave>

<wave number="2">
  <title>Wave 2: Exit Convenience & Return Portal Placement (ENTRY-02, ENTRY-03)</title>
  <tasks>
    <task type="auto">
      <name>Task 2.1: Create ReturnPortalTile</name>
      <files>Tiles/ReturnPortalTile.cs</files>
      <instruction>
        Create `Tiles/ReturnPortalTile.cs` as a custom `ModTile`:
        - Set static defaults: `Main.tileSolid[Type] = false; Main.tileFrameImportant[Type] = true; Main.tileNoAttach[Type] = true; Main.tileLavaDeath[Type] = false;`
        - Object data: `TileObjectData.newTile.CopyFrom(TileObjectData.Style1x1); TileObjectData.addTile(Type);`
        - Map entry: `AddMapEntry(new Color(100, 200, 255), CreateMapEntryName());`
        - `RightClick(int i, int j)`:
          - If `BossArenaRoutingRegistry.IsAnyArenaActive()`:
            - `Main.NewText("메인 월드로 귀환합니다.", 180, 220, 255);`
            - `SubworldSystem.Exit();`
            - `return true;`
          - `return false;`
        - Create placeholder asset `Tiles/ReturnPortalTile.png`.
      </instruction>
    </task>
    <task type="auto">
      <name>Task 2.2: Add ReturnPortal placement to ArenaBuilder and ArenaPolishPass</name>
      <files>Subworlds/ArenaBuilder.cs, Subworlds/ArenaPolishPass.cs</files>
      <instruction>
        1. In `Subworlds/ArenaBuilder.cs`:
           - Add `public static void PlaceReturnPortal(int x, int y, ushort portalTileType)` using `WorldGen.PlaceTile(x, y, portalTileType, mute: true, forced: true);`.
        2. In `Subworlds/ArenaPolishPass.cs`:
           - In `ApplyPass()`: resolve `ushort portalType = (ushort)ModContent.TileType<Tiles.ReturnPortalTile>();`
           - Call `ArenaBuilder.PlaceReturnPortal((Main.maxTilesX / 2) + 4, _surfaceY - 1, portalType);` to place the portal tile on top of the base platform next to spawn.
      </instruction>
    </task>
  </tasks>
  <checkpoint>
    - Verify with `dotnet build BossArenaSubWorld.csproj`.
    - Report Wave 2 deliverables to user for inspection & approval.
  </checkpoint>
</wave>

</waves>

<verification>
- `dotnet build BossArenaSubWorld.csproj` exits 0 with 0 errors and 0 warnings.
- `BossSummonPlayer.OnEnterWorld()` does not call `NPC.SpawnOnPlayer`.
- `Tiles/ReturnPortalTile.cs` calls `SubworldSystem.Exit()`.
- `ArenaPolishPass` places `ReturnPortalTile`.
</verification>
