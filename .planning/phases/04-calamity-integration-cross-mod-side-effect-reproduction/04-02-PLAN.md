---
phase: 04-calamity-integration-cross-mod-side-effect-reproduction
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: false
requirements: [MOD-01, APPLY-02, APPLY-03]

must_haves:
  truths:
    - "Using the Hive Mind BossCoreItem on a fresh, dedicated CalamityMod-enabled test world converts a placed Aerialite Ore (Disenchanted) tile into real Aerialite Ore, and shows the Sky Ore chat broadcast (APPLY-02, APPLY-03 empirically confirmed, per D-04)"
    - "The mod loads and runs without a JIT crash or exception when CalamityMod is disabled, confirming the [JITWhenModsEnabled]/ModLoader.HasMod isolation boundary actually holds at runtime, not just in code review (Success Criterion 4, per D-05)"
  artifacts: []
  key_links: []
---

<objective>
Empirically prove, via two real in-game checkpoints, that Plan 04-01's Hive Mind integration satisfies this phase's remaining Success Criteria: the WorldGen ore-conversion side effect actually fires on a live world (D-04), and the mod remains safe to load with CalamityMod disabled (D-05).

Purpose: This project has no automated test framework for in-game tModLoader behavior (per 04-VALIDATION.md) -- `CalamityNetcode.SyncWorld()` is a singleplayer no-op and `AerialiteOreGen.Enchant()` permanently mutates world tiles, so both effects can only be confirmed by a live playthrough, not a build-time check. Per D-04, the WorldGen test uses a throwaway dedicated test world (NOT the backed-up main save Phase 3 used), since ore-tile conversion is a permanent terrain change.
Output: Two completed manual checkpoint results (WorldGen conversion + Sky Ore broadcast confirmed; Calamity-disabled load-safety confirmed), recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-calamity-integration-cross-mod-side-effect-reproduction/04-CONTEXT.md
@.planning/phases/04-calamity-integration-cross-mod-side-effect-reproduction/04-RESEARCH.md
@.planning/phases/04-calamity-integration-cross-mod-side-effect-reproduction/04-VALIDATION.md
@docs/WORLD_BACKUP_GUIDANCE.md
@Integrations/CalamityIntegration.cs
@Systems/BossRegistry.cs
@Items/BossCoreItem.cs
@Tiles/Test1Tile.cs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live WorldGen + netcode/messaging side-effect verification (D-04, APPLY-02, APPLY-03)</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/CalamityIntegration.cs (Plan 04-01 -- ApplyHiveMindDowned's AerialiteOreGen.Enchant() + CalamityUtils.BroadcastLocalizedText + CalamityNetcode.SyncWorld() replay being exercised)
    - Systems/BossRegistry.cs, Items/BossCoreItem.cs (existing carrier-item pipeline being reused unchanged from Phase 3)
    - .planning/phases/04-calamity-integration-cross-mod-side-effect-reproduction/04-CONTEXT.md D-04 (fresh dedicated test world requirement -- NOT the backed-up main save, since WorldGen effects permanently alter terrain)
    - .planning/phases/04-calamity-integration-cross-mod-side-effect-reproduction/04-RESEARCH.md section "D-02 Resolution: Hive Mind, with evidence" point 6 (confirms Aerialite Ore (Disenchanted) tiles are placed unconditionally by AerialiteOreGen.Generate()'s GenPass on any fresh Calamity-enabled world -- no special seed/settings needed, just CalamityMod enabled)
  </read_first>
  <action>
    Create a brand-new, throwaway Terraria world (NOT the real save "HiPo's_Terrarium") with CalamityMod, SubworldLibrary, and BossArenaSubWorld all enabled, world evil type = Corruption (Hive Mind's own Teratoma summon item is Corruption-flavored). Then, in that world:

    1. Place a Test1 portal tile (per Phase 2's SUBW-02 pattern) somewhere accessible.
    2. Obtain a Teratoma (Calamity's Corruption pre-Hardmode summon item -- check its exact crafting recipe via the in-game crafting menu; it requires Corruption-biome materials at a Demon Altar).
    3. Before killing Hive Mind, locate at least one placed "Aerialite Ore (Disenchanted)" tile -- Aerialite spawns on floating sky islands per Calamity's WorldGen, so use the full map or fly to a floating island -- and note its exact appearance (darker/desaturated ore sprite) for a before/after comparison.
    4. Right-click the Test1 tile while holding the Teratoma to redirect into the boss-arena subworld; Hive Mind auto-summons (per Phase 2's SUBW-04 auto-summon-on-arrival pattern).
    5. Kill Hive Mind. Confirm a BossCoreItem drops (per the existing DROP-02 pipeline, unchanged this phase).
    6. Pick up the BossCoreItem, exit the subworld via SubworldLibrary's Return button (back to the main/test world).
    7. Use the BossCoreItem (left-click while selected in your hotbar). Confirm ALL of:
       a. The chat message "Boss credential applied: calamity:hive_mind" appears (BossCoreItem.UseItem's existing Applied-case message, unchanged from Phase 3).
       b. The Calamity Sky Ore broadcast chat message (localization key `Mods.CalamityMod.Status.Progression.SkyOreText`, cyan-colored) ALSO appears -- this is APPLY-02's messaging side effect firing.
       c. Return to the sky-island Aerialite tile noted in step 3 (or check nearby floating islands generally) and confirm it has visually converted from the disenchanted variant to the real Aerialite Ore tile. `AerialiteOreGen.Enchant()` iterates the full map width, so ALL placed disenchanted tiles should now be converted, not just the one you were tracking.
  </action>
  <what-built>
    Plan 04-01 built Integrations/CalamityIntegration.cs, registering Hive Mind into the existing BossRegistry/SummonItemRegistry pipeline with a faithful replay of Hive Mind's real OnKill() side effects: the WorldGen ore-enchant (AerialiteOreGen.Enchant()), the Sky Ore chat broadcast (CalamityUtils.BroadcastLocalizedText), and the netcode sync (CalamityNetcode.SyncWorld()) -- not just the raw downedHiveMind boolean. This task verifies those side effects actually fire correctly on a real world, live, not just in a code review of the decompiled source.
  </what-built>
  <how-to-verify>
    Follow the 7 numbered steps in the action above. `CalamityNetcode.SyncWorld()` itself has no independently observable singleplayer effect (gated on `Main.dedServ` per the decompiled source) -- its correctness is confirmed by the surrounding code executing without error, not a separate visible check.
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader WorldGen/netcode behavior (per 04-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - A fresh, throwaway world (not the real save "HiPo's_Terrarium") was used, with CalamityMod + SubworldLibrary + BossArenaSubWorld enabled
    - Hive Mind was killed inside the boss-arena subworld (not the main/test world directly) and a BossCoreItem dropped
    - Using the BossCoreItem produced BOTH the "Boss credential applied: calamity:hive_mind" message AND the Calamity Sky Ore broadcast message
    - At least one previously-disenchanted Aerialite Ore tile is confirmed visually converted to the real Aerialite Ore tile after using the BossCoreItem
  </acceptance_criteria>
  <done>APPLY-02 (netcode/messaging side effect) and APPLY-03 (WorldGen side effect) are empirically confirmed for Hive Mind via a real in-game kill/carry/apply cycle on a throwaway dedicated world, per D-04.</done>
  <resume-signal>Report the result of each numbered step. Type "worldgen verified" if the Sky Ore chat message and the Aerialite tile conversion both occurred as expected, or describe exactly what went wrong (which step, what was observed instead) otherwise.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Calamity-disabled load-safety checkpoint (D-05, Success Criterion 4)</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/CalamityIntegration.cs (the ModLoader.HasMod("CalamityMod") guard and the two [JITWhenModsEnabled("CalamityMod")]-tagged methods being exercised)
    - .planning/phases/04-calamity-integration-cross-mod-side-effect-reproduction/04-CONTEXT.md D-05 (a real in-game checkpoint is required -- not code-review-only confidence, since JIT crashes aren't reliably caught by code review alone)
  </read_first>
  <action>
    In tModLoader's Mod Configuration menu, disable CalamityMod (leave SubworldLibrary and BossArenaSubWorld enabled), then relaunch tModLoader and load into any world (the throwaway world from Task 1, or a fresh one -- no world data is at risk since this test only exercises mod-load JIT behavior, not gameplay). Confirm ALL of:

    1. tModLoader loads to the main menu and into the world without a JIT crash, exception dialog, or mod-load error mentioning "CalamityMod".
    2. BossArenaSubWorld's other, CalamityMod-independent features (the King Slime pipeline from Phase 3, the Test1 tile from Phase 2) still function normally with CalamityMod absent -- this confirms `CalamityIntegration.PostSetupContent()`'s `if (!ModLoader.HasMod("CalamityMod")) return;` guard correctly short-circuits before either `[JITWhenModsEnabled("CalamityMod")]`-tagged method (`RegisterHiveMind`, `ApplyHiveMindDowned`) is ever JIT-compiled.
    3. Check tModLoader's client log (`Logs/client.log` under the tModLoader install/save directory) for any exception mentioning "CalamityMod", "TypeLoadException", or "MissingMethodException" -- confirm none are present.

    Re-enable CalamityMod afterward to restore the normal mod list for future testing/phases.
  </action>
  <what-built>
    Plan 04-01's Integrations/CalamityIntegration.cs isolates every CalamityMod-type-touching method behind `[JITWhenModsEnabled("CalamityMod")]`, guarded by an `ModLoader.HasMod("CalamityMod")` check in `PostSetupContent()` that never itself touches a Calamity type (per D-01's strict isolation discipline). This task verifies that isolation boundary actually prevents a JIT crash at runtime when CalamityMod is genuinely absent, not just that the code looks correctly isolated on review.
  </what-built>
  <how-to-verify>
    Follow the 3 numbered steps in the action above, in tModLoader's Mod Configuration menu and client log.
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game mod-load JIT behavior (per 04-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - tModLoader launched and loaded into a world successfully with CalamityMod disabled, no JIT crash or exception dialog observed
    - BossArenaSubWorld's Phase 2/3 features (Test1 tile, King Slime pipeline) still function with CalamityMod absent
    - Logs/client.log contains no "CalamityMod"-related TypeLoadException or MissingMethodException entries
    - CalamityMod re-enabled afterward, mod list confirmed restored to normal
  </acceptance_criteria>
  <done>Success Criterion 4 ("the mod continues to load and run safely with CalamityMod disabled") is empirically confirmed via a real in-game load test, per D-05, not just code review.</done>
  <resume-signal>Type "load-safety verified" if tModLoader loaded cleanly with CalamityMod disabled and the log is clean, or paste the exact exception/error otherwise.</resume-signal>
</task>

</tasks>

<verification>
This plan's entire verification is its two live checkpoints -- no automated test framework exists for in-game tModLoader behavior (per 04-VALIDATION.md). No repo files are modified by this plan; it is a pure verification gate closing out Phase 4.
</verification>

<success_criteria>
- Live test confirms Hive Mind's netcode/messaging side effect (Sky Ore chat broadcast) and WorldGen side effect (Aerialite ore conversion) both fire correctly when the BossCoreItem is used, on a throwaway dedicated test world per D-04 -- satisfying APPLY-02 and APPLY-03.
- Live test confirms the mod loads and runs safely with CalamityMod disabled, with no JIT crash and no CalamityMod-related exceptions in the client log, per D-05 -- satisfying this phase's Success Criterion 4.
- Every requirement this phase owns (MOD-01, APPLY-02, APPLY-03) is confirmed satisfied end-to-end, closing out Phase 4 per REQUIREMENTS.md's Traceability table.
</success_criteria>

<output>
After completion, create `.planning/phases/04-calamity-integration-cross-mod-side-effect-reproduction/04-02-SUMMARY.md`, recording the actual live test results for both checkpoints and confirmation of this phase's Success Criteria.
</output>
