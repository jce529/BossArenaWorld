---
phase: 10-full-calamity-spirit-boss-roster-registration-and-biome-subworld-routing
plan: 06
type: execute
wave: 5
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05"]
files_modified: []
autonomous: false
requirements: [ARENA-01]

must_haves:
  truths:
    - "Every one of the 18 bosses registered across Plans 10-02..10-05 is confirmed, by live in-game test, to set its correct downed flag and replay its documented side effects when its BossCoreItem is used in the main world"
    - "The Infernum-conditional registration matrix (Providence/Profaned Guardians/Ceaseless Void register only when InfernumMode is absent; The Old Duke registers only when InfernumMode is present; Astrum Deus/Astrum Aureus force night only when InfernumMode is present) is confirmed correct in BOTH mod configurations, not just one"
    - "Moon Jelly Wizard and Dusking's forced night persists for a full multi-minute fight without a mid-fight daytime despawn (Pitfall 6)"
    - "The mod continues to load and run safely with CalamityMod disabled and (separately) SpiritMod disabled -- no JITException naming any Phase 10 registration method"
  artifacts: []
  key_links: []
---

<objective>
Live-verify the full roster registered across Plans 10-01 through 10-05: 12 Calamity bosses (Providence,
Profaned Guardians, Ceaseless Void, The Old Duke, Signus, Storm Weaver, Astrum Deus, Astrum Aureus,
Dragonfolly, Devourer of Gods, Yharon, Supreme Witch Calamitas) and 6 Spirit bosses (Ancient Avian,
Scarabeus, Vinewrath Bane, Moon Jelly Wizard, Dusking, Atlas), plus the two new architecture pieces
(polymorphic `MarkofProvidence` resolution, forced-night persistence) and the Infernum-conditional
registration matrix.

Per `10-VALIDATION.md`'s Sampling Rate, this checkpoint covers the required minimum set rather than every
single boss individually (an 18-boss exhaustive live-fight checklist is impractical in one sitting): one
Zone-functional boss from each mod (Dragonfolly, Scarabeus), one Zone-thematic-only boss, both forced-night
bosses for their full fight duration, the polymorphic item across all reachable Zones, and the full
Infernum-conditional matrix in both mod configurations. All other registrations are code-verified via
`dotnet build` in their respective plans (10-02 through 10-05) -- this checkpoint is the live-behavior
confirmation layer on top of that, matching this project's established Phase 4/5/6/9 checkpoint-plan
pattern.

Purpose: Confirms ARENA-01 is satisfied end-to-end for Phase 10's full roster, not just internally
consistent at compile time.
Output: None (no files modified) -- this plan is entirely live verification checkpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-full-calamity-spirit-boss-roster-registration-and-biome-subworld-routing/10-CONTEXT.md
@.planning/phases/10-full-calamity-spirit-boss-roster-registration-and-biome-subworld-routing/10-RESEARCH.md
@.planning/phases/10-full-calamity-spirit-boss-roster-registration-and-biome-subworld-routing/10-VALIDATION.md
@docs/WORLD_BACKUP_GUIDANCE.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Registration-matrix + representative live-fight verification</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/CalamityIntegration.cs, Integrations/SpiritIntegration.cs (final state after Plans 10-02..10-05, the full set of registrations being exercised)
    - Systems/SummonItemRegistry.cs (RegisterPolymorphic/TryGetBoss, Plan 10-01)
    - docs/WORLD_BACKUP_GUIDANCE.md (backup procedure before touching a real save)
  </read_first>
  <action>
    Before starting, back up the world save per `docs/WORLD_BACKUP_GUIDANCE.md` (or use a fresh throwaway world). Enable CalamityMod, SpiritMod, and InfernumMode alongside BossArenaSubWorld, SubworldLibrary, CheatSheet, and BossChecklist.

    1. **Zone-functional bosses (must NOT despawn/underperform):**
       - Spawn `CalamityMod.Items.SummonItems.ExoticPheromones` (CheatSheet), right-click the Test1 portal tile -> confirm redirect into `BossArenaJungleSubworld`, Dragonfolly auto-summons, fight it for at least 1-2 minutes and confirm it does NOT despawn/enrage from a leave-Jungle timer.
       - Spawn `SpiritMod.Items.Consumable.ScarabIdol`, right-click Test1 -> confirm redirect into `BossArenaDesertSubworld`, Scarabeus auto-summons, confirm normal (non-nerfed) damage output in both directions.
       - Kill both, return to main world (SubworldLibrary Return), use both dropped `BossCoreItem`s, confirm `CalamityMod.DownedBossSystem.downedDragonfolly == true` and `SpiritMod.MyWorld.DownedScarabeus == true` (BossChecklist tracker UI or CheatSheet variable inspection).
    2. **One Zone-thematic-only boss (sanity check the plain routing path):**
       - Spawn `SpiritMod.Items.Consumable.JewelCrown`, right-click Test1 -> confirm redirect into `BossArenaSpaceSubworld`, Ancient Avian auto-summons and fights normally. Kill it, apply the `BossCoreItem`, confirm `SpiritMod.MyWorld.DownedAncientAvian == true`.
    3. **Polymorphic MarkofProvidence resolution (test all 3 reachable Zones):**
       - Spawn `CalamityMod.Items.SummonItems.MarkofProvidence`. With InfernumMode DISABLED for this sub-test: stand in the Dungeon, right-click Test1 -> confirm redirect summons Ceaseless Void (not Signus/Storm Weaver). Stand in the Underworld (Y position), right-click Test1 -> confirm Signus. Stand in Space (near the top of the world), right-click Test1 -> confirm Storm Weaver. Confirm the item is never consumed across all 3 uses.
       - Kill at least one of the three, apply its `BossCoreItem`, confirm the correct `DownedBossSystem` flag flips (`downedCeaselessVoid`/`downedSignus`/`downedStormWeaver` matching whichever was killed).
    4. **Infernum-conditional registration matrix (both configurations):**
       - With InfernumMode DISABLED: confirm `CalamityMod.Items.SummonItems.ProfanedCore` (Providence), `ProfanedShard` (Profaned Guardians), and `MarkofProvidence`-in-the-Dungeon (Ceaseless Void) all redirect normally via Test1. Confirm `InfernumMode.Content.Items.SummonItems.BloodwormPlatter` (The Old Duke) does NOT redirect at all (no summon item registered -- Test1 right-click with it held should fall through with no interaction, since InfernumMode itself isn't even loaded to provide the item).
       - With InfernumMode ENABLED: confirm `ProfanedCore`/`ProfanedShard`/`MarkofProvidence`-in-the-Dungeon now produce NO redirect (fall through untouched to Infernum's own structure-gated flow -- no chat message, item not consumed). Confirm `BloodwormPlatter` NOW redirects normally via Test1, The Old Duke auto-summons, and killing it + applying its `BossCoreItem` sets `CalamityMod.DownedBossSystem.downedBoomerDuke == true` (NOT `downedOldDuke`) plus the Sea King shop unlock.
       - With InfernumMode ENABLED: confirm Astrum Deus (`Starcore`) and Astrum Aureus (`AstralChunk`) both force night in `BossArenaAstralSubworld` on arrival (visibly dark, `Main.dayTime == false`). With InfernumMode DISABLED, confirm neither forces night (normal daytime arena is fine).
    5. **Re-use idempotency spot-check:**
       - Pick any one already-downed boss's `BossCoreItem` and use it again -- confirm no duplicate chat broadcast/WorldGen mutation (APPLY-04).
  </action>
  <what-built>
    All 18 new bosses (12 Calamity + 6 Spirit) registered end-to-end via `Integrations/CalamityIntegration.cs`/`Integrations/SpiritIntegration.cs`, routed to their correct arena subworlds, plus the new `SummonItemRegistry.RegisterPolymorphic` (MarkofProvidence -> Ceaseless Void/Signus/Storm Weaver) and the Infernum-conditional registration matrix (D-02).
  </what-built>
  <how-to-verify>
    Follow the 5 numbered steps in the action above, in order.
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader boss-kill/flag-application/Infernum-toggle verification (per 10-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - Dragonfolly does not despawn/enrage during a 1-2 minute fight in `BossArenaJungleSubworld`, and `downedDragonfolly` flips true after BossCoreItem use
    - Scarabeus deals/takes normal (non-1/3-scaled) damage in `BossArenaDesertSubworld`, and `DownedScarabeus` flips true after BossCoreItem use
    - Ancient Avian fights normally in `BossArenaSpaceSubworld` and `DownedAncientAvian` flips true after BossCoreItem use
    - MarkofProvidence resolves to Ceaseless Void/Signus/Storm Weaver correctly in all 3 tested Zones, item never consumed
    - With InfernumMode disabled: Providence/Profaned Guardians/Ceaseless Void redirect normally, The Old Duke does not redirect at all
    - With InfernumMode enabled: Providence/Profaned Guardians/Ceaseless Void produce no redirect, The Old Duke redirects normally and sets `downedBoomerDuke` (not `downedOldDuke`)
    - Astrum Deus/Astrum Aureus force night only when InfernumMode is enabled
    - Re-using an already-downed boss's BossCoreItem produces no duplicate side effect
  </acceptance_criteria>
  <done>All 5 verification steps pass with no discrepancy from the expected behavior described in each step.</done>
  <resume-signal>Type "all verified" if every item above passes, or describe exactly which item failed and how (e.g. "MarkofProvidence in Underworld spawned Storm Weaver instead of Signus").</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Forced-night persistence -- full-duration fight (Pitfall 6)</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Systems/ForcedTimeSystem.cs (the per-tick re-assertion mechanism being verified, Plan 10-01)
    - .planning/phases/10-full-calamity-spirit-boss-roster-registration-and-biome-subworld-routing/10-RESEARCH.md "Pitfall 6: Forced-night persistence during a live fight"
  </read_first>
  <action>
    Spawn `SpiritMod.Items.Consumable.DreamlightJellyItem` (Moon Jelly Wizard) or `SpiritMod.Items.Consumable.DuskCrown` (Dusking), right-click Test1, and fight the boss for its FULL natural duration (do not intentionally rush) -- long enough to exceed several real-world minutes if possible. Watch specifically for the boss suddenly despawning/going inactive partway through (the `Main.dayTime` -> `active = false` despawn check confirmed in both bosses' AI, 10-RESEARCH.md). Confirm the screen stays visibly dark/night-colored for the entire fight, not just at the moment of arrival.

    If a mid-fight despawn IS observed: this confirms `Main.time` does advance naturally inside a SubworldLibrary subworld faster than `ForcedTimeSystem.PreUpdateWorld()`'s per-tick reset can counteract (unlikely given the reset runs every tick, but flag it explicitly if seen) -- note exactly when in the fight it happened.
  </action>
  <what-built>
    `Systems/ForcedTimeSystem.cs`'s `PreUpdateWorld()` re-asserts `Main.dayTime = false` / `Main.time = 0.0` every tick while inside a boss arena for a force-night boss, specifically to survive a fight that runs long enough for in-game time to naturally advance into daytime if only set once on entry.
  </what-built>
  <how-to-verify>
    Follow the steps in the action above for at least one of Moon Jelly Wizard or Dusking (ideally both, if time permits).
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for a multi-minute live in-game fight duration check (per 10-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - The screen stays visibly dark/night-colored for the entire fight duration
    - The boss does not despawn/go inactive from a `Main.dayTime == true` state at any point mid-fight
  </acceptance_criteria>
  <done>At least one of Moon Jelly Wizard/Dusking is confirmed to survive its full natural fight duration without a daytime-triggered despawn.</done>
  <resume-signal>Type "forced night confirmed persistent" if the boss never despawned from daytime across the full fight, or describe when/how a despawn occurred.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CalamityMod-disabled / SpiritMod-disabled JIT safety</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/CalamityIntegration.cs, Integrations/SpiritIntegration.cs (confirm every method added across Plans 10-02..10-05 carries a `[JITWhenModsEnabled(...)]` tag before starting this checkpoint)
    - .planning/PROJECT.md Key Decisions (the Phase 4 JIT-crash lesson, commit `0e19600` -- named delegates only, never inline lambdas)
  </read_first>
  <action>
    In tModLoader's Mod Configuration menu:
    1. Disable ONLY CalamityMod (leave SpiritMod, InfernumMode, and everything else enabled/unchanged). Relaunch or reload. Confirm no crash dialog, no `JITException`, and check `Logs/client.log` for any exception naming `CalamityIntegration` or any of this phase's new method names (`RegisterProvidence`, `ResolveMarkOfProvidenceBoss`, `RegisterOldDuke`, etc.). Re-enable CalamityMod afterward.
    2. Disable ONLY SpiritMod (leave everything else enabled). Relaunch or reload. Confirm no crash dialog, no `JITException`, and check `Logs/client.log` for any exception naming `SpiritIntegration` or its new method names (`ApplyGenericSpiritDowned`, `RegisterVinewrathBane`, etc.). Re-enable SpiritMod afterward.
    3. Confirm both mods are re-enabled and the mod list matches its pre-checkpoint state before finishing.
  </action>
  <what-built>
    Every method in `Integrations/CalamityIntegration.cs`/`Integrations/SpiritIntegration.cs` added across Plans 10-02 through 10-05 is tagged `[JITWhenModsEnabled("CalamityMod")]`/`[JITWhenModsEnabled("SpiritMod")]`/`[JITWhenModsEnabled("InfernumMode")]` as appropriate, with every delegate passed into `BossRegistry.Register`/`SummonItemRegistry.RegisterPolymorphic` being a named method (never an inline lambda), per the Phase 4 JIT-crash lesson (commit `0e19600`).
  </what-built>
  <how-to-verify>
    Follow the 3 numbered steps in the action above, in order.
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for a live tModLoader mod-disabled load/crash check (per 10-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - Disabling CalamityMod alone produces no crash dialog and no JITException naming CalamityIntegration in Logs/client.log
    - Disabling SpiritMod alone produces no crash dialog and no JITException naming SpiritIntegration in Logs/client.log
    - Both mods end re-enabled, matching the pre-checkpoint mod list state
  </acceptance_criteria>
  <done>Both mod-disabled load tests complete cleanly with no JITException, and the mod list is restored to its pre-checkpoint state.</done>
  <resume-signal>Type "mod-disabled safety verified" if both disabled-load tests passed cleanly, or paste the relevant exception text from Logs/client.log if either crashed.</resume-signal>
</task>

</tasks>

<verification>
This plan is entirely live in-game verification -- there is no automated command beyond the `dotnet build` gates already passed in Plans 10-01 through 10-05. Success is defined by the three checkpoint tasks' `resume-signal`s all confirming pass, per `10-VALIDATION.md`'s "Phase gate" sampling requirement (one Zone-functional boss per mod, one thematic-only boss, both forced-night bosses full-duration, polymorphic item across all reachable zones, Infernum-conditional gating in both configurations, and mod-disabled JIT safety for both CalamityMod and SpiritMod).
</verification>

<success_criteria>
- All items in Task 1's registration-matrix + representative live-fight checklist pass
- Task 2's full-duration forced-night fight shows no mid-fight daytime despawn for Moon Jelly Wizard or Dusking
- Task 3's CalamityMod-disabled and SpiritMod-disabled load tests both complete with no JITException
- ARENA-01 is satisfied for this phase's full researched Calamity/Spirit roster
</success_criteria>

<output>
After completion, create `.planning/phases/10-full-calamity-spirit-boss-roster-registration-and-biome-subworld-routing/10-06-SUMMARY.md`
</output>
