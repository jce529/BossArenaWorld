---
phase: 05-spirit-integration
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: []
autonomous: false
requirements: [MOD-02]

must_haves:
  truths:
    - "Using the Infernon BossCoreItem on a fresh, dedicated SpiritMod-enabled test world sets MyWorld.DownedInfernon to true and reproduces the HellstoneBrick/lava tile-ring WorldGen side effect at the player's position (MOD-02 Success Criterion 1, empirically confirmed per D-05)"
    - "A deliberately broken reflection field name degrades gracefully -- Mod.Logger.Warn fires and BossCoreItem.UseItem does not throw or crash (MOD-02 Success Criterion 1's reflection-failure path, 05-VALIDATION.md manual-only verification)"
    - "The mod loads and runs without a JIT crash or exception when SpiritMod is disabled, confirming the [JITWhenModsEnabled]/ModLoader.HasMod isolation boundary actually holds at runtime, not just in code review (Success Criterion 3, per D-05)"
  artifacts: []
  key_links: []
---

<objective>
Empirically prove, via three real in-game checkpoints, that Plan 05-01's Infernon integration satisfies Phase 5's remaining Success Criteria: the downed-flag + WorldGen side effect actually applies on a live world (D-05), the reflection-based write path fails gracefully rather than crashing, and the mod remains safe to load with SpiritMod disabled (D-05).

Purpose: This project has no automated test framework for in-game tModLoader behavior (per 05-VALIDATION.md) -- reflection into an internal field, `NetMessage.SendData(MessageID.WorldData)` (singleplayer no-op), and `WorldGen.SquareTileFrame` tile mutation can only be confirmed by a live playthrough, not a build-time check. Per D-05, all three checkpoints use a throwaway dedicated test world (NOT the backed-up main save Phase 3/4 used), since the WorldGen tile-ring is a permanent terrain change and SpiritMod is not currently installed on the main save's mod list.
Output: Three completed manual checkpoint results (downed-flag + WorldGen replay confirmed; reflection-failure graceful-degradation confirmed; SpiritMod-disabled load-safety confirmed), recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-spirit-integration/05-CONTEXT.md
@.planning/phases/05-spirit-integration/05-RESEARCH.md
@.planning/phases/05-spirit-integration/05-VALIDATION.md
@docs/WORLD_BACKUP_GUIDANCE.md
@Integrations/SpiritIntegration.cs
@Systems/BossRegistry.cs
@Items/BossCoreItem.cs
@Tiles/Test1Tile.cs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live downed-flag + WorldGen side-effect verification (D-05, MOD-02 Success Criterion 1)</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/SpiritIntegration.cs (Plan 05-01 -- ApplyInfernonDowned's reflection write into BossDownedTracker.Downed + ReplayInfernonTileRing's HellstoneBrick/lava tile mutation being exercised)
    - Systems/BossRegistry.cs, Items/BossCoreItem.cs (existing carrier-item pipeline being reused unchanged from Phase 3)
    - .planning/phases/05-spirit-integration/05-CONTEXT.md D-05 (fresh dedicated test world requirement -- NOT the backed-up main save, since SpiritMod is not currently enabled on it and the WorldGen tile-ring permanently alters terrain)
    - .planning/phases/05-spirit-integration/05-RESEARCH.md section "Environment Availability" (SpiritMod's `.tmod` is not currently present/enabled locally -- must be re-subscribed via Steam Workshop/Mod Browser before this checkpoint can run at all)
  </read_first>
  <action>
    First, if SpiritMod is not currently installed, re-subscribe to it via Steam Workshop / tModLoader's in-game Mod Browser and enable it.

    Create a brand-new, throwaway Terraria world (NOT the real save "HiPo's_Terrarium") with SpiritMod, SubworldLibrary, and BossArenaSubWorld all enabled. Then, in that world:

    1. Place a Test1 portal tile (per Phase 2's SUBW-02 pattern) somewhere accessible.
    2. Obtain a Cursed Cloth (SpiritMod's Infernon summon item -- craft it: 4 Soul of Night + 3 Carved Rock + 3 Hellstone Bar at a Mythril/Orichalcum Anvil, or use the in-game creative menu / debug item-spawn if available on this test world).
    3. Before killing Infernon, note that `MyWorld.DownedInfernon` should currently read false (fresh world) -- if BossChecklist is also installed, its Infernon entry should show undefeated.
    4. Right-click the Test1 tile while holding the Cursed Cloth to redirect into the boss-arena subworld; Infernon auto-summons (per Phase 2's SUBW-04 auto-summon-on-arrival pattern).
    5. Kill Infernon (in either Normal or Expert Mode -- note which). Confirm a BossCoreItem drops (per the existing DROP-02 pipeline, unchanged this phase) regardless of whether the final blow was dealt to Infernon itself or a spawned InfernoSkull.
    6. Pick up the BossCoreItem, exit the subworld via SubworldLibrary's Return button (back to the main/test world).
    7. Note your current in-world position (this is where the tile-ring replay will appear).
    8. Use the BossCoreItem (left-click while selected in your hotbar). Confirm ALL of:
       a. The chat message "Boss credential applied: spirit:infernon" appears (BossCoreItem.UseItem's existing Applied-case message, unchanged from Phase 3).
       b. A ring of HellstoneBrick tiles with lava fills the ring's interior now appears centered on the position noted in step 7 (the WorldGen replay, anchored on the player per D-02's nuance -- NOT at Infernon's original death position in the subworld).
       c. `MyWorld.DownedInfernon` now reads true -- check via BossChecklist's UI if installed, or via a temporary debug `Main.NewText(SpiritMod.MyWorld.DownedInfernon.ToString())` call if not (revert any temporary debug code added for this check before finishing).
  </action>
  <what-built>
    Plan 05-01 built Integrations/SpiritIntegration.cs, registering Infernon into the existing BossRegistry/SummonItemRegistry pipeline with a reflection-based write into SpiritMod's internal BossDownedTracker.Downed dictionary (no public setter exists) and a faithful replay of Infernon's real WorldGen tile-ring side effect, anchored at the player's position. This task verifies those side effects actually fire correctly on a real world, live, not just in a code review of the decompiled source.
  </what-built>
  <how-to-verify>
    Follow the 8 numbered steps in the action above.
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader WorldGen/reflection behavior (per 05-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - A fresh, throwaway world (not the real save "HiPo's_Terrarium") was used, with SpiritMod + SubworldLibrary + BossArenaSubWorld enabled
    - Infernon was killed inside the boss-arena subworld (not the main/test world directly) and a BossCoreItem dropped
    - Using the BossCoreItem produced the "Boss credential applied: spirit:infernon" message
    - A HellstoneBrick/lava tile ring appeared at the player's position (not the subworld death position) after using the BossCoreItem
    - `MyWorld.DownedInfernon` reads true after using the BossCoreItem
  </acceptance_criteria>
  <done>MOD-02's Success Criterion 1 (downed flag applies correctly, WorldGen side effect reproduced) is empirically confirmed for Infernon via a real in-game kill/carry/apply cycle on a throwaway dedicated world, per D-05.</done>
  <resume-signal>Report the result of each numbered step. Type "infernon downed verified" if the credential message, tile-ring replay, and MyWorld.DownedInfernon=true all occurred as expected, or describe exactly what went wrong (which step, what was observed instead) otherwise.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Reflection-failure graceful-degradation checkpoint (05-RESEARCH.md Open Question 1)</name>
  <files>none -- verification only; any temporary code edit made during this task must be reverted before finishing</files>
  <read_first>
    - Integrations/SpiritIntegration.cs (the ApplyInfernonDowned try/catch block and the `_downedField` lookup being exercised)
    - .planning/phases/05-spirit-integration/05-VALIDATION.md "Manual-Only Verifications" table (this exact checkpoint's specified test instructions)
  </read_first>
  <action>
    Temporarily edit `Integrations/SpiritIntegration.cs`'s `RegisterInfernon()` method, changing the
    reflected field name from `"Downed"` to an intentionally wrong value, e.g. `"DoesNotExist"`:
    ```csharp
    _downedField = ModLoader.GetMod("SpiritMod").Code
        .GetType("SpiritMod.NPCs.BossDownedTracker")
        .GetField("DoesNotExist", BindingFlags.NonPublic | BindingFlags.Static);
    ```
    Rebuild (`dotnet build BossArenaSubWorld.csproj`) and reload the test world from Task 1 (or any
    world with SpiritMod enabled and a `spirit:infernon` BossCoreItem in inventory -- if none is
    available, kill Infernon again first per Task 1's steps 1-6).

    Use the BossCoreItem. Confirm ALL of:
    1. No crash, no exception dialog, no client freeze.
    2. `Logs/client.log` contains a warning line matching "SpiritIntegration: failed to set Infernon downed flag via reflection" (the `Mod.Logger.Warn` call in the catch block).
    3. The chat message "Boss credential applied: spirit:infernon" STILL appears (BossCoreItem.UseItem's Applied-case message fires regardless of the internal ApplyDowned failure, since BossRegistry.Apply() has no failure-signaling path back from ApplyDowned -- this is the documented, accepted swallow-and-log design from 05-RESEARCH.md Open Question 1).

    Afterward, revert `Integrations/SpiritIntegration.cs`'s field name back to `"Downed"` and rebuild
    to confirm `dotnet build BossArenaSubWorld.csproj` still exits 0 with the real field name restored.
  </action>
  <what-built>
    Plan 05-01's ApplyInfernonDowned wraps the reflection write in a try/catch, logging via
    `Mod.Logger.Warn` on failure rather than propagating the exception into `BossCoreItem.UseItem`.
    This task verifies that graceful-degradation path actually holds under a real, deliberately
    broken reflection lookup, not just in code review.
  </what-built>
  <how-to-verify>
    Follow the numbered steps in the action above, checking the client log and in-game chat.
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game tModLoader reflection-failure behavior (per 05-VALIDATION.md); this is a manual-only, code-review-assisted live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - Using the BossCoreItem with a deliberately broken field name produced no crash/exception dialog
    - Logs/client.log contains the "SpiritIntegration: failed to set Infernon downed flag via reflection" warning
    - The "Boss credential applied: spirit:infernon" chat message still appeared despite the internal failure
    - Integrations/SpiritIntegration.cs's field name was reverted to "Downed" and `dotnet build BossArenaSubWorld.csproj` exits 0 afterward
  </acceptance_criteria>
  <done>The reflection-failure path is empirically confirmed to degrade gracefully (log + no-op, no crash) rather than breaking the carrier-item pipeline, and the real implementation is restored and building cleanly.</done>
  <resume-signal>Type "reflection failure verified" if the warning logged, no crash occurred, and the field name was reverted with a clean rebuild, or describe exactly what went wrong otherwise.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live SpiritMod-disabled load-safety checkpoint (D-05, Success Criterion 3)</name>
  <files>none -- verification only, no repo files modified</files>
  <read_first>
    - Integrations/SpiritIntegration.cs (the ModLoader.HasMod("SpiritMod") guard and the four [JITWhenModsEnabled("SpiritMod")]-tagged methods being exercised)
    - .planning/phases/05-spirit-integration/05-CONTEXT.md D-05 (a real in-game checkpoint is required -- not code-review-only confidence, since JIT crashes aren't reliably caught by code review alone; mirrors Phase 4's identical checkpoint)
  </read_first>
  <action>
    In tModLoader's Mod Configuration menu, disable SpiritMod (leave CalamityMod, SubworldLibrary,
    and BossArenaSubWorld in whatever state they were previously), then relaunch tModLoader and load
    into any world (the throwaway world from Task 1, or a fresh one -- no world data is at risk
    since this test only exercises mod-load JIT behavior, not gameplay). Confirm ALL of:

    1. tModLoader loads to the main menu and into the world without a JIT crash, exception dialog,
       or mod-load error mentioning "SpiritMod".
    2. BossArenaSubWorld's other, SpiritMod-independent features (the King Slime pipeline from
       Phase 3, the Test1 tile from Phase 2, and Hive Mind from Phase 4 if CalamityMod is enabled)
       still function normally with SpiritMod absent -- this confirms
       `SpiritIntegration.PostSetupContent()`'s `if (!ModLoader.HasMod("SpiritMod")) return;` guard
       correctly short-circuits before any of the four `[JITWhenModsEnabled("SpiritMod")]`-tagged
       methods (`RegisterInfernon`, `IsInfernonDowned`, `ApplyInfernonDowned`,
       `ReplayInfernonTileRing`) is ever JIT-compiled.
    3. Check tModLoader's client log (`Logs/client.log` under the tModLoader install/save
       directory) for any exception mentioning "SpiritMod", "TypeLoadException", or
       "MissingMethodException" -- confirm none are present.

    Re-enable SpiritMod afterward to restore the normal mod list for future testing/phases.
  </action>
  <what-built>
    Plan 05-01's Integrations/SpiritIntegration.cs isolates every SpiritMod-type-touching method
    behind `[JITWhenModsEnabled("SpiritMod")]`, guarded by a `ModLoader.HasMod("SpiritMod")` check
    in `PostSetupContent()` that never itself touches a Spirit type (per D-01's strict isolation
    discipline, identical to Phase 4's proven pattern). This task verifies that isolation boundary
    actually prevents a JIT crash at runtime when SpiritMod is genuinely absent, not just that the
    code looks correctly isolated on review.
  </what-built>
  <how-to-verify>
    Follow the 3 numbered steps in the action above, in tModLoader's Mod Configuration menu and
    client log.
  </how-to-verify>
  <verify>
    <automated>MISSING -- no automated test framework exists for in-game mod-load JIT behavior (per 05-VALIDATION.md); this is a manual-only live checkpoint by design</automated>
  </verify>
  <acceptance_criteria>
    - tModLoader launched and loaded into a world successfully with SpiritMod disabled, no JIT crash or exception dialog observed
    - BossArenaSubWorld's Phase 2/3/4 features still function with SpiritMod absent
    - Logs/client.log contains no "SpiritMod"-related TypeLoadException or MissingMethodException entries
    - SpiritMod re-enabled afterward, mod list confirmed restored to normal
  </acceptance_criteria>
  <done>Success Criterion 3 ("the mod continues to load and run safely with Spirit disabled") is empirically confirmed via a real in-game load test, per D-05, not just code review.</done>
  <resume-signal>Type "load-safety verified" if tModLoader loaded cleanly with SpiritMod disabled and the log is clean, or paste the exact exception/error otherwise.</resume-signal>
</task>

</tasks>

<verification>
This plan's entire verification is its three live checkpoints -- no automated test framework exists for in-game tModLoader behavior (per 05-VALIDATION.md). No repo files are permanently modified by this plan (Task 2's temporary field-name edit is reverted within the same task); it is a pure verification gate closing out Phase 5.
</verification>

<success_criteria>
- Live test confirms Infernon's downed flag (`MyWorld.DownedInfernon`) and WorldGen tile-ring side effect both apply correctly when the BossCoreItem is used, on a throwaway dedicated test world per D-05 -- satisfying MOD-02 Success Criterion 1.
- Live test confirms a deliberately broken reflection lookup degrades gracefully (log + no crash), validating the swallow-and-log design from 05-RESEARCH.md Open Question 1.
- Live test confirms the mod loads and runs safely with SpiritMod disabled, with no JIT crash and no SpiritMod-related exceptions in the client log, per D-05 -- satisfying this phase's Success Criterion 3.
- MOD-02 is confirmed satisfied end-to-end, closing out Phase 5 per REQUIREMENTS.md's Traceability table.
</success_criteria>

<output>
After completion, create `.planning/phases/05-spirit-integration/05-02-SUMMARY.md`, recording the actual live test results for all three checkpoints and confirmation of this phase's Success Criteria.
</output>
